appId: com.jacklee.videodownloader
productName: VideoDownloader
executableName: VideoDownloader
copyright: Copyright © Jack Lee
directories:
  output: release
  buildResources: build
files:
  - dist/**/*
  - dist-electron/**/*
  - package.json
extraResources:
  - from: resources/bin
    to: bin
    filter:
      - "**/*"
      - "!**/.gitkeep"
  - from: resources/sidecar
    to: sidecar
    filter:
      - "**/*"
      - "!**/.gitkeep"
protocols:
  - name: VideoDownloader
    schemes:
      - videodl
mac:
  icon: build/icon.png
  category: public.app-category.utilities
  target:
    - dmg
    - dir
  darkModeSupport: true
  hardenedRuntime: true
  gatekeeperAssess: false
  identity: null
  extendInfo:
    # 仅改 DisplayName；CFBundleName 必须保持与 productName/Helper 一致，
    # 否则 Electron 会 FATAL: Unable to find helper app（启动即崩溃）。
    CFBundleDisplayName: 视频下载器
    # 允许把 URL / .webloc 拖到 Dock 图标（配合 main.ts 的 open-file 处理；需打包后真机验证）
    CFBundleDocumentTypes:
      - CFBundleTypeName: 网页链接
        CFBundleTypeRole: Viewer
        LSHandlerRank: Alternate
        LSItemContentTypes:
          - public.url
          - public.url-name
          - com.apple.web-internet-location
dmg:
  artifactName: VideoDownloader-${version}-mac.${ext}
  title: 视频下载器 ${version}
