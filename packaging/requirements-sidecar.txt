# coding: utf-8
# Sidecar 运行时（无 Qt）
# default：把与本版本严格匹配的 yt-dlp-ejs 求解脚本打进 Sidecar，
# 避免 Windows 客户端每次依赖 GitHub 临时下载 YouTube challenge 脚本。
# curl-cffi：Pornhub 等站点需浏览器 TLS 指纹，否则网页拉取会 410 Gone。
yt-dlp[default,curl-cffi]==2026.7.4
pyinstaller>=6.3.0
