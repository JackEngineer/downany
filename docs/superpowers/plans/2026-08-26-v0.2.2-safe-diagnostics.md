# v0.2.2 安全诊断包实施计划

> 目标：让首次使用阶段的诊断导出默认采用结构化摘要和数据最小化，并以失败测试证明原实现会泄漏、以压缩包全文审计证明新实现不携带敏感正文。

**设计文档：** `docs/superpowers/specs/2026-08-26-v0.2.2-safe-diagnostics-design.md`

## Task 1：锁定当前泄漏行为

- [x] 扩展 `tests/sidecar/test_diagnostics.py`，构造包含 URL 查询 Token、Cookie、Authorization、Bot Token、私密标题、原始响应正文及 Windows/macOS 路径的日志和失败任务。
- [x] 断言压缩包只允许四个结构化成员，全文不出现任一敏感哨兵。
- [x] 运行该测试并确认在未修改生产代码时失败，失败原因来自原始内容实际进入压缩包。

命令：

```powershell
& 'D:\work\downany\venv\Scripts\python.exe' -m pytest tests/sidecar/test_diagnostics.py -q
```

## Task 2：实现最小化诊断输出

- [x] 修改 `src/sidecar/diagnostics.py`：版本探测错误和非结构化输出只返回稳定状态。
- [x] 环境信息删除可执行文件和 ffmpeg 绝对路径，改为来源与可用性字段。
- [x] 失败任务改为按允许的错误码和平台聚合，不保留逐任务 URL、标题和原始错误。
- [x] 原始日志复制改为仅统计文件数、可读文件数、行数和级别。
- [x] 写入固定 `privacy.json`，并保持现有 Sidecar 返回合同兼容。
- [x] Sidecar 导出异常收敛为稳定、可重试的协议错误，不拼接底层异常。
- [x] 运行目标测试直至通过。

## Task 3：让界面准确说明结果

- [x] 为设置页诊断说明增加一个先失败的源码合同测试，要求文案说明包含版本、运行环境、日志统计和失败类型，并明确不包含下载链接、标题和日志正文。
- [x] 增加失败文案合同，禁止把内部异常原样渲染给用户。
- [x] 修改 `desktop/renderer/SettingsApp.tsx` 的用户文案，不展示内部实现术语。
- [x] 运行目标 Renderer 测试并确认通过。

## Task 4：回归与证据

- [x] 运行 `pytest tests/sidecar -q`：66 项通过；最终全量复跑已覆盖新增 handler 测试。
- [x] 运行 `pytest tests/core tests/data tests/sidecar -q`：最终 242 项通过。
- [x] 运行 `cd desktop; npm.cmd test`：最终 49 个文件、275 项通过。
- [x] 运行 `cd desktop; npm.cmd run build`：TypeScript 与生产构建通过。
- [x] 用独立临时目录导出一份真实诊断 ZIP：四个固定成员，敏感哨兵扫描为零。
- [x] 检查相对基线的 diff、工作区状态和提交范围。
- [ ] 提交到本地 `fix/v0.2.2-safe-diagnostics` 分支；不合并、不推送。

## 完成边界

本计划完成后，只能说明“安全诊断包”这一 v0.2.2 独立增量已经本地实现并验证。完整 v0.2.2 仍需安装入口分支集成、README/路线图事实同步、Windows 首次使用可见验收、真实 macOS 安装启动验收、版本准备和三件同源发布物验证。
