# v0.2.2 首次使用安装包冒烟实施计划

> 目标：用现有生产协议把“进程活着”升级为“Sidecar、诊断导出、扩展桥、入队和任务可见性真实可用”的跨平台安装包冒烟。

**设计文档：** `docs/superpowers/specs/2026-08-26-v0.2.2-first-use-smoke-design.md`

## Task 1：先锁定黑盒 helper 合同

- [x] 新建 `scripts/package_smoke_helpers.test.mjs`，先导入尚不存在的 helper 模块并观察 `ERR_MODULE_NOT_FOUND`。
- [x] 测试桥端口未占用、未就绪重试、就绪成功和子进程提前退出。
- [x] 测试入队成功合同与 `/tasks` 同 ID、非 `unknown` 合同。
- [x] 用内存 ZIP fixture 测试固定成员、截断、重复成员、加密、非法 UTF-8、越界中央目录和本地文件头名称不一致。

命令：

```powershell
node --test scripts/package_smoke_helpers.test.mjs
```

## Task 2：实现无依赖 helper

- [x] 新建 `scripts/package_smoke_helpers.mjs`。
- [x] 所有 HTTP 请求支持注入 `fetch`、明确超时和限长响应正文。
- [x] 等待循环同时观察子进程退出状态，不使用固定睡眠判定成功。
- [x] ZIP 解析只返回经过 UTF-8、加密标记、本地头、重复项和边界检查的成员。
- [x] 运行 Task 1 测试直至通过。

## Task 3：增强打包 Electron 冒烟

- [x] 重构 `scripts/test_packaged_electron.mjs` 使用 helper，并保留现有命令行参数兼容。
- [x] 启动前确认桥未被其他进程占用。
- [x] 隔离 Electron user-data 与 Downany data 目录，并关闭更新检查。
- [x] 等待真实 `/health` 就绪，再经 `/enqueue` 和 `/tasks` 验证固定 smoke 任务。
- [x] 所有退出路径收敛进程树和自建临时目录；实跑后端口、候选进程和近期自建临时目录均为零。

## Task 4：增强打包 Sidecar 冒烟

- [x] 在 `scripts/test_packaged_sidecar.mjs` 中调用 `app.exportDiagnostics`。
- [x] 校验返回路径在指定数据目录内、ZIP 为非空普通文件。
- [x] 用同一 ZIP helper 校验四个固定成员。
- [x] 保留 Telegram offset 与优雅 shutdown 现有检查。

## Task 5：接入普通 CI 与验证

- [x] 在 `.github/workflows/ci.yml` 的桌面 job 中运行 helper 单元测试；本地 `js-yaml` 解析通过。
- [x] 运行 Node 20.20.2 helper 测试：7 项通过；三个脚本 `node --check` 通过。
- [x] 运行完整 Python 测试：243 项通过；桌面测试：49 个文件、275 项通过；生产构建通过；扩展三项基线通过。
- [x] 用当前源码构建 Windows onedir Sidecar，Node 20.20.2 增强 smoke 通过；主 EXE 13,348,488 bytes，SHA256 `59a260840d69638de4eb9e40d14047ab77c6446a014d50ca231ce07c77247986`。
- [x] 用当前源码、当前 Sidecar 和已校验 ffmpeg/yt-dlp 构建 Windows `win-unpacked`；固定 `electronDist=node_modules/electron/dist` 后构建成功，Node 20.20.2 增强 Electron smoke 通过。
- [x] `Downany.exe` 188,784,128 bytes，SHA256 `c581cc900958af04dd0f17fbd8383d882a3c68a9a995e2fa53077eebb1313276`；`app.asar` 9,013,690 bytes，SHA256 `a83be2459b47c80d86b6461e0121673e079e6ae07c82bacb8e77741692e7763a`。
- [x] 打包 Sidecar 导出的验收 ZIP 980 bytes，SHA256 `40548abe48d710dd6637984a11d50dc7a964ae4b14a4e4ad999bf9b783e27cef`，四个固定成员通过。
- [ ] 检查 diff、工作区和提交范围后提交本地分支；不合并、不推送。

## 完成边界

该计划不执行安装器安装/卸载，不创建 tag，不发布 Release，也不把本地 Windows 结果当作 macOS 通过。真实双平台可见验收继续保留为 v0.2.2 发布门槛。
