# v0.3.0 本地集成与 Windows 安装记录

记录日期：2026-08-28（Asia/Shanghai）。结论：已提交并合入本地 main，将现有 Windows 安装从 0.2.4 升级至 0.3.0；下述自动核验通过。未推送、打标签或发布。

## 授权与 Git 边界

- 用户“按照你的建议来”批准了本地提交与 main 集成、备份现有数据、原目录升级及自动核验；不包含远程写入、公开发布或 Mac 工作。
- 源码提交：`d5aff4aaecf75d77dce1d4c9eb28899eda1e7394`，父提交 `c7709bcc64035c18cf94efcfc62b40ad088b8f24`，包含 138 个相关文件。另一个文档提交记录本次结果，不改变产品代码。
- 本地 main 从 `0dbaeb7fb0394887be51f6dea773aaa79ae38546` 快进集成，在 `.worktrees/v0.3.0-main-integration` 核验。根工作区仍保留 `chore/v0-3-staged-roadmap` 及原有 7 个未提交规划文件，文件状态与哈希均未变。
- 候选分支、旧工作树和旧候选继续保留；没有删除分支、工作树或产物，也没有执行 pull、push、远程 CI 或发布操作。
- 385 个冻结源码输入的原始字节哈希未变，按仓库换行规则映射的 Git blob 与源码提交全部一致。原始候选与清单见 [历史候选验证](v0.3.0-stable-baseline.md)。

## 本轮重新执行的回归

| 检查 | 候选提交前 | 合入 main 后 |
| --- | --- | --- |
| Python 全量及必需媒体集成 | 771 passed / 126.53s | 771 passed / 136.17s |
| 桌面全量 | 490 passed / 70 files / 227.09s | 490 passed / 70 files / 85.30s |
| Main / Renderer 严格类型检查 | 通过 | 通过，生产构建成功 |
| 扩展 | 三个脚本通过 | 三个脚本通过 |
| 打包与布局辅助测试 | 25 + 12 passed | 25 + 12 passed |

首次并行复跑为 770 passed、1 failed：`test_download_rebinds_missing_system_proxy_before_starting` 使用假媒体却调用真实 FFmpeg 抽帧，日志显示返回前已超过测试的 3 秒等待门槛。保留失败日志；未改变源码、断言或超时，原用例单独连续三次通过，随后候选与 main 的完整串行回归均通过。这里不声称已修复该测试的时序敏感性。

独立 main 生产构建的七个 JS/CSS 文件与原候选逐字节相同；三个 HTML 文件仅有 CRLF/CRCRLF 换行空白差异，资源引用和其余文本一致。没有把“构建通过”写成所有输出逐字节一致，也没有重新生成或替换安装包。

## 备份与原目录升级

- 原安装为 `D:\downany`、全用户范围、系统登记 0.2.4。使用同一个已核验 NSIS 原目录覆盖，未先单独卸载或删除用户数据。
- 安装包 `Downany-0.3.0-win-x64.exe`：199531109 字节，SHA-256 `ea4749ed52edf8bfd350ad3bd0a7eebf17fafe7c5eba19b53e306bb2c3cae946`。
- 安装器于 13:02:55 启动，13:04:59 退出，退出码 0；使用 `/S /allusers /D=D:\downany`，没有 `--force-run`。等待过系统权限确认，没有绕过 UAC 或改变安全设置。
- 三个已知应用数据根中实际存在的两个目录共 161 个文件、31229590 字节，已完整备份并逐文件核验；包括配置、SQLite 主文件/WAL/SHM 和 Electron 配置缓存，不复制目录外的下载成品。
- 私有备份：`%LOCALAPPDATA%\DownanyBackups\before-0.3.0-20260828-125707-5ae8573f9c0343298fce94af4307356a`。权限仅保留当前用户、SYSTEM 与 Administrators，数据和凭据未进入 Git。
- 已核对并保留原 0.2.4 最新安装器作为回退材料：`release-v0.2.4-parse-ui-fix/Downany-0.2.4-win-x64.exe`，SHA-256 `fe2c8c0dbeb3dc7589280393b8db1d8968fe846fd80951e37ae3ccf15de47ae4`。

## 安装态自动核验

| 项目 | 结果 |
| --- | --- |
| 安装文件同一性 | 222 个候选包内文件与 `D:\downany` 对应文件的大小和 SHA-256 全部一致 |
| 版本 | 卸载登记、ASAR 均为 0.3.0；已安装 Sidecar 实际 hello 为 0.3.0，协议版本 1，收到 shutdown 确认并退出 0 |
| ASAR 内容 | 十个冻结编译输入全部一致；无开发 Gallery |
| 媒体工具与诊断 | 已安装 ffmpeg/ffprobe 真实样本生成和探测通过；Sidecar 协议、精确偏移量与诊断包检查通过 |
| 隔离真实启动 | 启动 `D:\downany\Downany.exe`，只使用隔离 profile 参数，未附加 `--no-sandbox` 或 `--disable-gpu`；Sidecar 与本地桥就绪 |
| 浏览器桥入队 | 本地测试 URL 入队成功，查询返回同一 taskId，状态为 pending；不是外部网站下载完成证明 |
| 用户数据保留 | 安装及自动核验后，原 161 个用户数据文件的集合、大小和 SHA-256 与备份全部一致 |
| 协议与快捷方式 | 当前用户的 `downany://` 关联由已安装应用自身注册为 `"D:\downany\Downany.exe" "%1"`；四个桌面/开始菜单快捷方式均指向该安装版 |
| 进程与端口 | 测试结束后 Downany/Sidecar/安装器进程 0，17888 监听 0；只结束本次创建的测试进程 |

静默安装不会自动运行程序，而原卸载步骤会清除旧协议关联；因此首次静态检查发现关联尚不存在。正常首次启动负责注册，本轮用隔离数据启动已安装程序完成该步骤，没有手写注册表或加载真实任务。

另发现个人桌面和个人开始菜单的两个历史快捷方式仍指向已不存在的 `windows-m0-git` 测试目录。先将二进制快捷方式备份至私有备份下的 `shortcuts-before` 并核验，再更新这两个失效入口的目标、工作目录和图标；公共快捷方式未修改。原快捷方式可以从备份恢复，没有删除旧候选文件。

## 证据与未执行范围

本机逐项证据位于候选工作树 `.build/v030-local-integration/20260828-r1/`：源码与 Git 映射、回归日志、main 构建比较、安装器状态、222 文件清单、激活结果、Sidecar 版本、备份复核和快捷方式记录。该目录被忽略，不包含在源码提交内；真实用户数据备份不进入该证据目录或 Git。

- 运行时测试使用新建隔离配置、数据库和 Electron profile，并关闭更新、剪贴板监视与自动发送。没有使用真实用户队列执行下载或 Telegram 发送，也没有宣称已完成真实用户 profile 的启动验收。
- 未进行人工验收、物理系统重启、真实网站网络矩阵、Mac 构建/DMG/验收、签名或原生 Telegram 模式核验。
- 未推送、打标签、上传、公开 Release、部署官网或执行公开下载验证。本地安装完成不等于正式发布完成。
