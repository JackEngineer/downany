# v0.3.0 公开发布验收

记录时间：2026-08-31 21:00 CST。状态：`v0.3.0` GitHub Release 与官网生产部署已公开，发布后检查通过。

## 发布身份

- 发布提交：`17e8a015d21f8eb040d2b93b388269b592cfc9c2`。
- 注解标签：`v0.3.0`，标签对象 `dcd3d797e394a0dbe7efeb38a686f4467ab9fcf6`，剥离后精确指向上述提交。
- GitHub Release：[`Downany 0.3.0`](https://github.com/JackEngineer/downany/releases/tag/v0.3.0)，Release ID `379755883`，于 `2026-08-31T12:55:59Z` 公开；`draft=false`、`prerelease=false`，并设为 Latest。
- 官网生产部署：Cloudflare Pages `205185d2-0e4c-4731-b848-8e027aeaf66a`，环境 `Production`、分支 `main`、源码 `17e8a01`。

## 发布门禁

| 门禁 | 运行 | 结果 |
| --- | --- | --- |
| `main` 常规 CI | [33392402152](https://github.com/JackEngineer/downany/actions/runs/33392402152) | macOS / Windows Python、桌面端、Chrome 扩展全部通过 |
| `main` cloud-only 双平台真实打包 | [33392794940](https://github.com/JackEngineer/downany/actions/runs/33392794940) | 两个平台真实媒体后处理、安装包构建、包内媒体、Sidecar 与 Electron 烟测全部通过 |
| `v0.3.0` 标签 CI | [33393330354](https://github.com/JackEngineer/downany/actions/runs/33393330354) | 标签提交上的同组门禁全部通过；正式 DMG / EXE 取自本次运行 |

本地最终回归同时通过：Python `770 passed, 3 skipped`；桌面端 `70` 个测试文件、`490` 项测试与生产构建；官网 `25` 项组件测试、`4` 项托管适配测试、TypeScript 检查与生产构建；Chrome 扩展测试与发布 ZIP 完整性检查。

首次 cloud-only 运行 `33391736842` 曾在两端真实媒体封面门禁失败。根因是开发/CI 依赖未包含 yt-dlp `default` extra 提供的 `mutagen`，而 Sidecar 打包依赖已包含；提交 `17e8a01` 对齐两套运行依赖并增加合同测试。上述三次成功运行均在修复后执行，不复用失败运行的产物。

## 公开资产

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `Downany-0.3.0-mac.dmg` | `180642959` | `6183ae8446e11ef9fd6d7a417317c0d4009b523003bb6fba17a21580ac339623` |
| `Downany-0.3.0-win-x64.exe` | `198996016` | `db2c3df75e9097573c3525c58e7a1c98ba276c29ad60faa22d6d7fef9adaa8cb` |
| `Downany-chrome-extension-0.8.2.zip` | `62658` | `9bbe8bbd3917dfcde26141167b4816f7e6da453614571ef201855d56009cc656` |

发布前草稿中的三项远端字节数与本地文件逐项相等。发布后匿名请求 GitHub Latest Release API 返回 HTTP `200`、`tag_name=v0.3.0`、`draft=false`、`prerelease=false` 和上述精确三项资产；三个公开下载 URL 跟随重定向后均返回 HTTP `200`。

补充产物检查：

- DMG 通过 `hdiutil verify`，只读挂载后 `CFBundleShortVersionString=0.3.0`，主程序为 arm64，挂载包内 Electron 隔离启动输出 `33.4.11`。
- Windows 文件被识别为 PE32 GUI / Nullsoft Installer；Windows 原生标签 CI 已完成包内 ffmpeg、ffprobe、Sidecar 与 Electron 烟测。
- 扩展 ZIP 通过 `unzip -t`，根目录含 `manifest.json`，版本为 `0.8.2`，不含测试文件或隐藏临时文件。

## 官网公开验收

- 生产地址：[`downany.51soulslink.com`](https://downany.51soulslink.com/) 与 [`downany.pages.dev`](https://downany.pages.dev/)。两者均已切到新生产部署。
- 真实 Chrome 桌面视口检查：首页结构、图片、功能区、下载区与 FAQ 布局正常。
- `393 × 852` 手机视口检查：无横向溢出；折叠菜单可打开/关闭；FAQ 可展开；两个下载区均显示“当前公开版 v0.3.0”。
- 两个域名上的 macOS / Windows 下载入口均精确指向 `v0.3.0` 的 DMG / EXE。
- 页面自身控制台错误为 `0`。验收浏览器中的 Vue Devtools 扩展报告了重复安装错误，来源为 `chrome-extension://`，不属于官网资源或脚本。
- 上一生产部署 `75ef96ec-be43-4a40-b6ec-c6215deb09ea` 仍由 Cloudflare 保留，可用于生产回滚。

## 明确边界

- macOS 包未使用 Developer ID 签名、未公证。`codesign -dv` 显示 `Signature=adhoc`、无 Team ID；严格签名验证不通过，不作为正式签名证明。用户首次打开仍需按安装说明处理 Gatekeeper。
- Windows 包未做 Authenticode 签名，SmartScreen 可能提示未知发布者。
- 本次为 cloud-only 安装包，不携带 Telegram Local Bot API、ProcessHost 或应用凭据，也不包含本地 Bot API 的单文件 2 GB 上传能力。
- 已有原生 CI、历史 Windows 覆盖安装与本机 Mac 候选证据，但没有在本次公开资产上重新完成干净 Windows / Mac 实机人工安装，因此不把它写成已通过。
- 应用内更新仅检查 GitHub Release 并引导下载，不会自动替换安装。
