# Downany v0.3.1 验收记录

状态：进行中。此文件只记录已经实际执行的证据，空项不视为通过。

## 源码自动化基线（2026-09-11）

| 检查 | 结果 |
| --- | --- |
| Python 核心、数据、Sidecar、CLI | `773 passed, 3 skipped` |
| 桌面端 | `70` 个测试文件、`497 passed` |
| 桌面端类型检查与生产构建 | 通过 |
| Chrome 扩展 shared / sniff / bridge timeout | 通过 |
| v0.3.1 矩阵与判定规则 | `7 passed`；30 条结构校验通过；强制 `macos-arm64`、`windows-x64` 两套独立结果 |
| 候选执行器与媒体工具检查 | `30 passed`；包内 FFmpeg / FFprobe 媒体检查通过 |

## 可靠性矩阵

- 场景定义：[v0.3.1-reliability-matrix.json](v0.3.1-reliability-matrix.json)
- 校验矩阵：`node scripts/evaluate_v031_acceptance.mjs docs/acceptance/v0.3.1-reliability-matrix.json`
- 执行候选包：`node scripts/run_v031_reliability_matrix.mjs --executable=<候选包可执行文件绝对路径> --playwright-module=<Playwright 目录绝对路径> --matrix=<矩阵绝对路径> --results=<合并结果绝对路径> --target=macos-arm64|windows-x64 --expected-version=0.3.1`。需登录样本可显式增加 `--cookiefile=<已授权 Netscape cookies.txt>`；`--case=<用例 ID>` 可单条续跑。
- 判定结果：未执行
- YouTube：未执行
- Bilibili：未执行
- 抖音：未执行

执行器使用隔离配置和输出目录，逐条等待最终状态并用候选包内 FFprobe 探测、FFmpeg 完整解码。合并结果文件应有 60 项，每项包含 `target`、`id`、`outcome`、`artifactPlayable`，错误样本另含 `errorCode`。`target` 只能为 `macos-arm64` 或 `windows-x64`；不得写入 URL、Cookie、账号信息、私有路径或原始错误。

执行器隔离复验：已阻止旧版配置迁入验收目录，并修正 macOS `/var` 与 `/private/var` 的测试断言。使用公开 CC0 音视频样本完成一次诊断运行，实际经过候选应用启动、UI 入队、Sidecar 下载、隔离目录落盘、FFprobe 探测及 FFmpeg 完整解码，成品 `1,131,116` 字节，SHA-256 为 `797e880d38a800b1a54b3ec4a16159c7cd20a15e4ea7676a84d2793d8bdbf6f3`。此诊断只验证执行链路，不计入 YouTube 正式样本结果。

## 失败恢复

- 登录状态、代理、下载工具、下载位置定向入口：源码自动化测试通过，人工复验未执行。
- 重试选项保留：源码测试证明画质、输出目录、格式、音频模式、后处理、请求头和任务 Cookie 文件保留；代理、浏览器登录状态、限速和分片并发采用重试时的当前设置。包级快照证据待执行。
- 中英文提示及敏感信息保护：桌面端全量回归通过；最终候选包人工复验未执行。

## 5 人首次使用试用

- 添加任务前下载位置检查：源码自动化测试通过；目录不存在时自动创建，路径被文件占用或无法写入时保留输入并打开下载位置设置。最终候选包人工复验未执行。
- 扩展连接反馈：弹窗连接状态映射测试和脚本检查通过；安装引导页在真实浏览器中检测到本机桥，切换为“桌面端已连接”并隐藏重复下载按钮。最终扩展包复验未执行。

| 试用者 | 平台 | 独立完成 | 阻塞步骤 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | — | 未执行 | — | — |
| 2 | — | 未执行 | — | — |
| 3 | — | 未执行 | — | — |
| 4 | — | 未执行 | — | — |
| 5 | — | 未执行 | — | — |

## 最终候选包

已生成 macOS Apple Silicon `0.3.1` 工程候选包：`.build/v031-macos-candidate/Downany-0.3.1-mac.dmg`，大小约 `176 MiB`，SHA-256 为 `5d57bfbd9899806340f593d504489dd8321aa3d51f129674d71110d9e7d15d49`。应用、Sidecar、FFmpeg、FFprobe 均核对为 arm64；包内版本为 `0.3.1`；Sidecar 协议与诊断导出、媒体工具合成与解码检查通过。该包未签名且未含 Telegram 本地运行时，只作为工程候选，不能作为最终发布包。

整应用桥接冒烟未计为通过：本机已有 Downany 占用 `127.0.0.1:17888`，测试按隔离规则拒绝接管现有实例。矩阵诊断已从应用内部接口完成等价的启动、入队和下载链路。Sidecar 冷启动曾出现约 `27` 秒，随后包内复验约 `18` 秒通过，最终包仍需观察首次启动等待时间。

| 平台 | 首次安装 | v0.3.0 覆盖升级 | 设置/队列保留 | 暂停恢复 | 成品播放 | 不覆盖已有文件 |
| --- | --- | --- | --- | --- | --- | --- |
| macOS Apple Silicon | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 |
| Windows x64 | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 |

## 发布结论

未达到发布门槛。
