# Downany v0.3.1 验收记录

状态：进行中。此文件只记录已经实际执行的证据，空项不视为通过。

## 源码自动化基线（2026-09-12）

| 检查 | 结果 |
| --- | --- |
| Python 核心、数据、Sidecar、CLI | `773 passed, 3 skipped` |
| 桌面端 | `70` 个测试文件、`503 passed` |
| 桌面端类型检查与生产构建 | 通过 |
| Chrome 扩展 shared / sniff / bridge timeout | 通过 |
| v0.3.1 矩阵与判定规则 | `12 passed`；30 条结构校验通过；强制 `macos-arm64`、`windows-x64` 两套独立结果和单一候选包校验值 |
| 候选执行器、端口隔离与媒体工具检查 | Node 辅助检查 `77 passed, 3 skipped`，桌面桥测试 `15 passed`；包内 FFmpeg / FFprobe 媒体检查通过 |

## 可靠性矩阵

- 场景定义：[v0.3.1-reliability-matrix.json](v0.3.1-reliability-matrix.json)
- 校验矩阵：`node scripts/evaluate_v031_acceptance.mjs docs/acceptance/v0.3.1-reliability-matrix.json`
- 执行候选包：`node scripts/run_v031_reliability_matrix.mjs --executable=<候选包可执行文件绝对路径> --candidate-artifact=<DMG 或 NSIS 安装包绝对路径> --playwright-module=<Playwright 目录绝对路径> --matrix=<矩阵绝对路径> --results=<合并结果绝对路径> --target=macos-arm64|windows-x64 --expected-version=0.3.1`。需登录样本可显式增加 `--cookiefile=<已授权 Netscape cookies.txt>`；`--case=<用例 ID>` 可单条续跑。
- 部分结果：[v0.3.1-reliability-results.partial.json](v0.3.1-reliability-results.partial.json)
- 判定结果：未通过；macOS 已记录 18/30 条，Windows 0/30 条，仍缺 12 条 macOS 登录态/抖音证据和全部 Windows 证据
- YouTube：macOS 普通视频 5/5、集合 2/2 均完成下载和完整解码；已下架视频正确返回 `removed`；两个登录态样本未执行
- Bilibili：macOS 普通视频 5/5、集合 2/2 均完成下载和完整解码；非法视频地址正确返回 `unsupported`；两个登录态样本未执行
- 抖音：macOS 无 Cookie 样本返回 `need_login`，非法视频地址返回 `unsupported`；其余 8 个授权 Cookie 下载/集合样本未执行

执行器使用隔离配置和输出目录，逐条等待最终状态并用候选包内 FFprobe 探测、FFmpeg 完整解码。合并结果文件应有 60 项，每项包含 `target`、`candidateSha256`、`id`、`outcome`、`artifactPlayable`，错误样本另含 `errorCode`。同一平台只能出现一个候选安装包校验值；换包续跑时自动淘汰该平台旧候选结果。`target` 只能为 `macos-arm64` 或 `windows-x64`；不得写入 URL、Cookie、账号信息、私有路径或原始错误。

执行器隔离复验：已阻止旧版配置迁入验收目录，并修正 macOS `/var` 与 `/private/var` 的测试断言。使用公开 CC0 音视频样本完成一次诊断运行，实际经过候选应用启动、UI 入队、Sidecar 下载、隔离目录落盘、FFprobe 探测及 FFmpeg 完整解码，成品 `1,131,116` 字节，SHA-256 为 `797e880d38a800b1a54b3ec4a16159c7cd20a15e4ea7676a84d2793d8bdbf6f3`。此诊断只验证执行链路，不计入 YouTube 正式样本结果。

平台预探测使用候选依赖提供的公开回归地址，不保存 URL。yt-dlp `2026.02.04` 对 YouTube 为 5/5 可解析、Bilibili 为 0/5、抖音无 Cookie 为 0/5；升级到[官方稳定版 `2026.08.19`](https://github.com/yt-dlp/yt-dlp/releases/tag/2026.08.19)后，Bilibili 恢复为 4/5，YouTube 应用级真实下载通过。抖音仍要求新鲜 Cookie，应用级错误码为 `need_login`，后续必须使用有权访问的浏览器登录状态完成可下载样本。

## 失败恢复

- 登录状态、代理、下载工具、下载位置定向入口：源码自动化测试通过，人工复验未执行。
- 重试选项保留：源码测试证明画质、输出目录、格式、音频模式、后处理、请求头和任务 Cookie 文件保留；代理、浏览器登录状态、限速和分片并发采用重试时的当前设置。macOS 候选包已用 `720p` 任务验证 503 网络失败后点击“重试”，任务 ID、画质、格式、音频模式、后处理和输出目录均保持不变。
- 中英文提示及敏感信息保护：桌面端全量回归通过；最终候选包人工复验未执行。

macOS 包级恢复结果：[v0.3.1-macos-recovery-results.json](v0.3.1-macos-recovery-results.json)。该结果绑定候选 DMG SHA-256，实际覆盖 5 条链路：公开样本下载、同名重复下载不覆盖、503 失败后显式重试、暂停后关闭并重启应用继续、传输中断后自动 Range 续传。候选应用共启动 3 次；暂停恢复从非零字节继续，网络中断只注入一次，全部成品通过 FFprobe 探测和 FFmpeg 完整音视频解码，先前已完成文件哈希保持不变。此证据属于候选包自动化验收，不替代真实用户操作记录。

## 5 人首次使用试用

- 添加任务前下载位置检查：源码自动化测试通过；目录不存在时自动创建，路径被文件占用或无法写入时保留输入并打开下载位置设置。最终候选包人工复验未执行。
- 扩展连接反馈：弹窗连接状态映射测试和脚本检查通过；安装引导页在真实浏览器中检测到本机桥，切换为“桌面端已连接”并隐藏重复下载按钮。最终扩展包复验未执行。

| 试用者 | 平台 | 独立完成 | 阻塞步骤 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | — | 未执行 | — | — |
| 2 | — | 未执行 | — | — |
| 3 | — | 未执行 | — | — |
| 4 | — | 未执行 | — | — |
| 5 | — | 未执行 | — | — |

## 最终候选包

已生成 macOS Apple Silicon `0.3.1` 工程候选包：`.build/v031-macos-candidate-h264/Downany-0.3.1-mac.dmg`，大小 `181,554,790` 字节，SHA-256 为 `837e21d67f279b348b73105bea849eb31dcf4117831681f3cdaa55067e6b1e7a`。应用与 Sidecar 均核对为 arm64，包内版本为 `0.3.1`，yt-dlp 为 `2026.08.19`；Sidecar 协议与诊断导出、媒体工具合成与解码检查通过。真实集合样本曾暴露 MP4 容器内 AV1 无法由候选媒体运行时解码；格式策略改为优先 H.264/MP4 后，同一样本已重新下载并完整解码通过。该包未签名且未含 Telegram 本地运行时，只作为工程候选，不能作为最终发布包。

候选验收进程已改用系统分配的临时桥端口，不再与本机正式实例共享 `127.0.0.1:17888`。在正式实例保持运行时，隔离候选完成 YouTube、Bilibili 应用内入队和下载。Sidecar 冷启动曾出现约 `29` 秒，随后包内复验约 `18` 秒通过，最终包仍需观察首次启动等待时间。

覆盖升级输入已准备：GitHub 正式发布的 `Downany-0.3.0-mac.dmg` 为 `180,642,959` 字节，SHA-256 `6183ae8446e11ef9fd6d7a417317c0d4009b523003bb6fba17a21580ac339623`，与 Release 资产摘要一致且 `hdiutil verify` 通过。`scripts/run_v031_macos_upgrade.mjs` 会在隔离目录安装 v0.3.0、建立设置及完成/暂停队列，再替换为 v0.3.1 验证设置、队列、非零 Range 续传和已有成品哈希；复制过程已用测试证明保留 Electron 框架相对链接。该包级升级门尚未通过：v0.3.0 固定占用 `127.0.0.1:17888`，执行器在端口已有应用时会提前拒绝，未写成“覆盖升级通过”。

| 平台 | 首次安装 | v0.3.0 覆盖升级 | 设置/队列保留 | 暂停恢复 | 成品播放 | 不覆盖已有文件 |
| --- | --- | --- | --- | --- | --- | --- |
| macOS Apple Silicon | 未执行 | 未执行 | 未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 |
| Windows x64 | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 |

## 发布结论

未达到发布门槛。
