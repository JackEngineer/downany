# Downany v0.3.1 验收记录

状态：进行中。此文件只记录已经实际执行的证据，空项不视为通过。

## 源码自动化基线（2026-09-12）

| 检查 | 结果 |
| --- | --- |
| Python 核心、数据、Sidecar、CLI | `782 passed, 3 skipped` |
| 桌面端 | `70` 个测试文件、`504 passed` |
| 桌面端类型检查与生产构建 | 通过 |
| Chrome 扩展 shared / sniff / bridge timeout / 打包脚本 | 通过；候选 ZIP 自动排除测试和隐藏文件，并核对包内版本 |
| v0.3.1 矩阵与判定规则 | `12 passed`；30 条结构校验通过；强制 `macos-arm64`、`windows-x64` 两套独立结果和单一候选包校验值 |
| 候选产物清单门禁 | `7 passed`；Mac、Windows 和扩展三个候选产物的大小、SHA-256 均匹配，产物完整性门禁通过 |
| 候选执行器、端口隔离、产物清单与媒体工具检查 | Node 辅助检查 `102 passed, 3 skipped`，桌面桥测试 `15 passed`；包内 FFmpeg / FFprobe 媒体检查通过 |
| macOS v0.3.0 → v0.3.1 覆盖升级 | 包级自动化通过；设置、完成/暂停队列、非零 Range 续传、成品播放和已有文件防覆盖均通过；人工安装未执行 |
| Windows v0.3.0 → v0.3.1 首次安装与覆盖升级 | 包级自动化通过；正式版安装、候选安装、设置与队列保留、非零 Range 续传、成品播放、已有文件防覆盖及全新数据目录均通过；人工安装未执行 |

## 可靠性矩阵

- 场景定义：[v0.3.1-reliability-matrix.json](v0.3.1-reliability-matrix.json)
- 校验矩阵：`node scripts/evaluate_v031_acceptance.mjs docs/acceptance/v0.3.1-reliability-matrix.json`
- 执行候选包：`node scripts/run_v031_reliability_matrix.mjs --executable=<候选包可执行文件绝对路径> --candidate-artifact=<DMG 或 NSIS 安装包绝对路径> --playwright-module=<Playwright 目录绝对路径> --matrix=<矩阵绝对路径> --results=<合并结果绝对路径> --target=macos-arm64|windows-x64 --expected-version=0.3.1`。需登录样本可显式增加 `--cookiefile=<已授权 Netscape cookies.txt>`；`--case=<用例 ID>` 可单条续跑。
- 旧候选部分结果：[v0.3.1-reliability-results.partial.json](v0.3.1-reliability-results.partial.json)
- 当前候选部分结果：[v0.3.1-reliability-results.current.partial.json](v0.3.1-reliability-results.current.partial.json)
- 当前候选判定结果：未通过；macOS 30/30 条已完成，Windows 0/30 条。候选包变更会更换安装包校验值，旧候选结果只能作为历史诊断，不能计入当前候选发布门槛
- 当前候选 YouTube：普通视频 5/5、登录态会话 2/2、集合 2/2 均完成下载和完整解码；已删除视频正确返回 `removed`
- 当前候选 Bilibili：普通视频 5/5、登录态会话 2/2、集合 2/2 均完成下载和完整解码；非法视频地址正确返回 `unsupported`。超长合集样本因首条时长约 2 小时 9 分钟超过单例时限而更换为两个当前公开短合集，新样本均以最终候选包通过
- 当前候选抖音：普通视频 5/5、登录态会话 2/2、合集 2/2 均使用已授权浏览器 Cookie 完成下载和完整解码；非法视频地址正确返回 `unsupported`。实现已补齐 Cookie 向预解析、合集展开和实际下载的传递，并为登录态抖音合集增加分页解析回退
- 旧候选 YouTube：普通视频 5/5、集合 2/2 均完成下载和完整解码；已下架视频正确返回 `removed`；两个登录态样本未执行
- 旧候选 Bilibili：普通视频 5/5、集合 2/2 均完成下载和完整解码；非法视频地址正确返回 `unsupported`；两个登录态样本未执行
- 旧候选抖音：无 Cookie 样本返回 `need_login`，非法视频地址返回 `unsupported`；其余 8 个授权 Cookie 下载/集合样本未执行

执行器使用隔离配置和输出目录，逐条等待最终状态并用候选包内 FFprobe 探测、FFmpeg 完整解码。合并结果文件应有 60 项，每项包含 `target`、`candidateSha256`、`id`、`outcome`、`artifactPlayable`，错误样本另含 `errorCode`。同一平台只能出现一个候选安装包校验值；换包续跑时自动淘汰该平台旧候选结果。`target` 只能为 `macos-arm64` 或 `windows-x64`；不得写入 URL、Cookie、账号信息、私有路径或原始错误。

执行器隔离复验：已阻止旧版配置迁入验收目录，并修正 macOS `/var` 与 `/private/var` 的测试断言。使用公开 CC0 音视频样本完成一次诊断运行，实际经过候选应用启动、UI 入队、Sidecar 下载、隔离目录落盘、FFprobe 探测及 FFmpeg 完整解码，成品 `1,131,116` 字节，SHA-256 为 `797e880d38a800b1a54b3ec4a16159c7cd20a15e4ea7676a84d2793d8bdbf6f3`。此诊断只验证执行链路，不计入 YouTube 正式样本结果。

平台预探测使用候选依赖提供的公开回归地址，不保存 URL。yt-dlp `2026.02.04` 对 YouTube 为 5/5 可解析、Bilibili 为 0/5、抖音无 Cookie 为 0/5；升级到[官方稳定版 `2026.08.19`](https://github.com/yt-dlp/yt-dlp/releases/tag/2026.08.19)后，Bilibili 恢复为 4/5，YouTube 应用级真实下载通过。抖音仍要求新鲜 Cookie，应用级错误码为 `need_login`，后续必须使用有权访问的浏览器登录状态完成可下载样本。

## 失败恢复

- 登录状态、代理、下载工具、下载位置定向入口：源码自动化测试通过；macOS 候选包实际点击网络异常、登录要求、下载工具过旧和下载位置失效入口，均打开设置并聚焦对应控件，人工复验未执行。
- 重试选项保留：源码测试证明画质、输出目录、格式、音频模式、后处理、请求头和任务 Cookie 文件保留；代理、浏览器登录状态、限速和分片并发采用重试时的当前设置。macOS 候选包已用 `720p` 任务验证 503 网络失败后点击“重试”，任务 ID、画质、格式、音频模式、后处理和输出目录均保持不变。
- 中英文提示及敏感信息保护：桌面端全量回归通过；macOS 候选包已验证九类中文提示，自动化证据不保存测试网址、Cookie 或原始错误，人工复验未执行。

macOS 包级恢复结果：[v0.3.1-macos-recovery-results.json](v0.3.1-macos-recovery-results.json)。该结果绑定候选 DMG SHA-256，实际覆盖 5 条链路：公开样本下载、同名重复下载不覆盖、503 失败后显式重试、暂停后关闭并重启应用继续、传输中断后自动 Range 续传。候选应用共启动 3 次；暂停恢复从非零字节继续，网络中断只注入一次，全部成品通过 FFprobe 探测和 FFmpeg 完整音视频解码，先前已完成文件哈希保持不变。此证据属于候选包自动化验收，不替代真实用户操作记录。

macOS 包级错误提示结果：[v0.3.1-macos-error-ui-results.json](v0.3.1-macos-error-ui-results.json)。候选包实际完成 9 类失败呈现：网络异常、登录要求、不支持内容和内容已删除由真实或受控下载失败产生；私密内容、地区限制、下载工具过旧、额外验证和下载位置失效使用隔离队列注入，只证明最终包界面呈现，不冒充平台错误分类。网络、登录、下载工具和下载位置四个操作均实际打开设置，并把焦点放到代理开关、浏览器登录状态、下载工具更新和下载目录控件。包级验收首次发现设置数据加载前焦点会丢失，修复后以新候选包复验通过；安装副本入口与 DMG 内入口 SHA-256 一致。此证据属于候选包自动化验收，不替代人工操作记录。

## 5 人首次使用试用

- 人工记录模板：[v0.3.1-manual-acceptance.template.json](v0.3.1-manual-acceptance.template.json)；执行说明：[v0.3.1-manual-acceptance-guide.md](v0.3.1-manual-acceptance-guide.md)。判定命令会强制五位匿名首次用户、至少四位无指导完成、至少一位实际使用扩展并完成桌面端连接、双平台人工记录和同平台候选包校验值一致；模板空项不会被误判为通过。
- 添加任务前下载位置检查：源码自动化测试通过；macOS 候选包实际把下载位置设为已存在文件，界面阻止创建任务、保留原队列、打开并聚焦下载目录设置；改回可写目录后同一链接完成下载。最终候选包人工复验未执行。
- 扩展连接反馈：弹窗连接状态和安装引导页两种状态的纯函数测试通过；候选 ZIP 已在 Chrome for Testing 153 中实际加载。无界面模式下本地网络权限保持待确认，候选安装页会提示用户允许本地网络并提供“重新检测”；有界面隔离浏览器连接同一候选桌面端后，页面实际切换为“桌面端已连接”，显示就绪状态、保留重新检测并隐藏重复下载按钮。本机桥健康、Sidecar 就绪，运行中的 `0.3.1` 桌面端主程序、`app.asar` 和 Sidecar 与候选展开包逐项同哈希。此项属于候选包浏览器自动化，不计作五位真实试用者；详见 [v0.3.1-extension-connection-results.json](v0.3.1-extension-connection-results.json)。
- macOS 候选包首次下载结果：[v0.3.1-macos-first-install-results.json](v0.3.1-macos-first-install-results.json)。从最终 DMG 复制应用到隔离安装目录，以全新 HOME、Electron 用户目录和空数据目录启动；应用自动创建配置及 `Downloads/Downany`。门禁先验证不可用目录不会产生任务并能直达对应设置，再修复目录提交同一链接；等待任务、完成状态与“打开”入口均可见，成品通过完整音视频解码。安装副本入口与 DMG 内入口 SHA-256 一致。此结果属于候选包自动化验收，不计作下面 5 位真实试用者中的任何一位。

| 试用者 | 平台 | 独立完成 | 阻塞步骤 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | — | 未执行 | — | — |
| 2 | — | 未执行 | — | — |
| 3 | — | 未执行 | — | — |
| 4 | — | 未执行 | — | — |
| 5 | — | 未执行 | — | — |

## 最终候选包

候选产物清单：[v0.3.1-candidate-artifacts.json](v0.3.1-candidate-artifacts.json)。运行 `node scripts/evaluate_v031_candidate_artifacts.mjs docs/acceptance/v0.3.1-candidate-artifacts.json --verify-available` 可复核当前产物。macOS、Windows 和扩展三个产物的大小及 SHA-256 已全部匹配，产物完整性判定为 `releaseReady: true`；这只表示候选文件齐全且同一，不代表下文的可靠性矩阵与人工发布门槛已经通过。

Windows 候选由 [GitHub Actions 运行 34682473616](https://github.com/JackEngineer/downany/actions/runs/34682473616) 的 `cloud-only` 模式生成，七个任务全部通过，源码提交为 `81feb52`。`native-resources (windows-latest, win32-x64)` 在包内 Sidecar、媒体工具和 Electron 冒烟后生成 `Downany-0.3.1-win-x64.exe`，大小 `198,641,522` 字节，SHA-256 为 `3bfea08c7919b75ea78c84c9e6cba3c380edb35dbb7d94349fb2b2851df3a8da`。同一任务完成 0.3.0 首次安装与覆盖升级门禁；下载后的安装包哈希、字节数及候选清单与 CI 工件逐项一致。Windows 30 条真实网站矩阵尚未执行。

CI 的 `workflow_dispatch` Windows 构建会在 Sidecar、媒体工具和 Electron 包级冒烟通过后自动执行同一记录命令。上传的 `downany-installer-win32-x64` 工件同时包含安装包、展开目录和已写入 Windows 大小及 SHA-256 的候选清单，避免把错版或未验证的 NSIS 带入后续矩阵。

已生成 macOS Apple Silicon `0.3.1` 工程候选包：`.build/v031-macos-candidate-collection/Downany-0.3.1-mac.dmg`，大小 `181,524,426` 字节，SHA-256 为 `9914b72fd9ab38f63b0fef56d7750fd221b0d63e3e221997a8e3f2421301019c`。应用与 Sidecar 均核对为 arm64，包内版本为 `0.3.1`，yt-dlp 为 `2026.08.19`；DMG 完整性、首次安装、0.3.0 覆盖升级、九类错误提示与四个恢复入口、失败重试、暂停续传、成品完整解码和防覆盖均已用此校验值重新通过。当前候选已完成 macOS 30/30 条真实网站证据；仍需同源码 Windows 可靠性矩阵与五位真实用户验收。该包未签名且未含 Telegram 本地运行时，只作为工程候选，不能作为最终发布包。

配套 Chrome 扩展候选为 `.build/v031-macos-candidate-bili/Downany-chrome-extension-0.8.3.zip`，大小 `62,057` 字节，SHA-256 为 `0ca90645c1e5b89b54304dea7562178017b488b8a770e5ba31117ce80f2285b5`。该包已改由统一脚本生成，统一文件时间和权限并移除额外 ZIP 元数据；同一源码重复打包字节一致。MV3 清单版本 `0.8.3`、shared、sniff、桥超时、隐藏状态和打包测试均通过，压缩包不含测试文件、隐藏文件、`__MACOSX` 或 `.DS_Store`。候选 ZIP 已在 Chrome for Testing 153 中加载，且固定端口上的桌面端核心文件与候选展开包同哈希；无界面模式验证权限恢复提示，有界面模式验证已连接状态及重复下载按钮隐藏。此结果不替代五位真实用户试用。

候选验收进程已改用系统分配的临时桥端口，不再与本机正式实例共享 `127.0.0.1:17888`。隔离候选完成三个平台的应用内入队、下载和完整解码。Sidecar 冷启动曾出现约 `30` 秒，随后包内复验约 `18` 秒通过，最终包仍需观察首次启动等待时间。

macOS 覆盖升级结果：[v0.3.1-macos-upgrade-results.json](v0.3.1-macos-upgrade-results.json)。输入为 GitHub 正式发布的 `Downany-0.3.0-mac.dmg`，大小 `180,642,959` 字节，SHA-256 `6183ae8446e11ef9fd6d7a417317c0d4009b523003bb6fba17a21580ac339623`；候选为本页记录的 v0.3.1 DMG。执行器在隔离目录启动 v0.3.0，保存下载目录、`480p`、并发数 `2`、深色主题和关闭元数据嵌入等设置，建立一个已完成任务和一个带部分文件的暂停任务；复制替换为 v0.3.1 后，两项任务 ID 和状态均保留，暂停任务从 Range `31744` 继续并完成，升级前已有成品哈希保持不变。升级前后成品均通过 FFprobe 探测和 FFmpeg 完整音视频解码。该结果属于包级自动化验收，人工覆盖安装仍未执行。

Windows 首次安装与覆盖升级结果：[v0.3.1-windows-upgrade-results.json](v0.3.1-windows-upgrade-results.json)。输入为 GitHub 正式发布的 `Downany-0.3.0-win-x64.exe`，SHA-256 `db2c3df75e9097573c3525c58e7a1c98ba276c29ad60faa22d6d7fef9adaa8cb`；候选为本页记录的 v0.3.1 NSIS。执行器分别完成正式版和候选版静默安装并验证启动及关键文件，升级后保留下载目录、`480p`、并发数 `2`、深色主题和关闭元数据嵌入等设置，完成任务与暂停任务的 ID 和状态不变；暂停任务从 Range `15360` 继续并完成，升级前已有成品哈希保持不变。全新数据目录的候选首次启动还验证了配置创建、隔离 HOME 内默认下载目录、UI 添加链接、完成状态与“打开”入口。升级前、恢复后和首次安装产生的成品均通过 FFprobe 探测和 FFmpeg 完整音视频解码。该结果属于包级自动化验收，Windows 人工安装仍未执行。

| 平台 | 首次安装 | v0.3.0 覆盖升级 | 设置/队列保留 | 暂停恢复 | 成品播放 | 不覆盖已有文件 |
| --- | --- | --- | --- | --- | --- | --- |
| macOS Apple Silicon | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 |
| Windows x64 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 | 候选包自动化通过；人工未执行 |

## 发布结论

未达到发布门槛。
