# Downany v0.2.4-A 成品正确性候选验收记录

> **当前范围（2026-08-27）：** 用户已明确“mac你不用管”，按 [Windows 范围调整](../superpowers/specs/2026-08-27-downany-windows-execution-scope.md) 执行。Mac 构建和验收不再阻塞本任务，未执行项不标为通过。Windows 的真实安装态验收仍须完成。

> 记录日期：2026-08-27（Asia/Shanghai）
> 当前结论：当前源码回归与交付前完整差异审查通过，包含握手、最终成品大小和解析失败界面修复的 Windows 最新候选已完成包级及真实界面预验收；其中 MP4 另在 5KPlayer 中观察到实际播放进度。2026-08-27 已将现有 `D:\downany` 从 `0.1.0` 覆盖安装为该 `0.2.4` 候选，安装文件同一性、正常首次启动、Sidecar 就绪、原配置与历史/队列保留、快捷方式和协议注册均已核验。随后正常退出及进程、端口释放已通过；自动重启操作遇到桌面控制工具超时，未观察到新进程，当前安装版处于关闭状态，重启验收未通过。Windows 重启与七条完整旅程仍有缺口；Mac 已不在本任务范围，当前 **还不是可发布候选，也不可发版**。

## 源码与候选边界

- 分支：`fix/v0.2.4-output-correctness`
- 当前基线提交：`c7709bcc64035c18cf94efcfc62b40ad088b8f24`
- 候选提交：未创建。本轮没有取得提交授权，因此没有执行 `git add` 或 `git commit`。
- 当前工作树包含 16 个已修改文件：
  - `desktop/electron/main.ts`
  - `desktop/electron/sidecar.ts`
  - `desktop/package-lock.json`
  - `desktop/package.json`
  - `desktop/renderer/components/AddConfirmDialog.test.tsx`
  - `desktop/renderer/components/AddConfirmDialog.tsx`
  - `docs/superpowers/plans/2026-08-26-downany-v0-2-4-output-correctness.md`
  - `docs/superpowers/specs/2026-08-26-downany-v0-2-4-output-correctness-design.md`
  - `docs/superpowers/specs/2026-08-26-downany-v0-3-staged-upgrade-design.md`
  - `scripts/package_smoke_helpers.mjs`
  - `scripts/package_smoke_helpers.test.mjs`
  - `scripts/test_packaged_electron.mjs`
  - `src/core/download_manager.py`
  - `src/sidecar/protocol.py`
  - `tests/core/test_download_manager.py`
  - `tests/sidecar/test_protocol.py`
- 另有五个新增未提交路径：Electron 握手行为测试 `desktop/electron/sidecar.test.ts`、真实存储最终大小回归 `tests/core/test_completed_output_size_persistence.py`、补验计划 `docs/superpowers/plans/2026-08-27-downany-v024-output-size-persistence.md`、范围调整 `docs/superpowers/specs/2026-08-27-downany-windows-execution-scope.md` 与本验收记录。
- 应用版本源已观察为 `0.2.4`；Electron 启动时通过 `app.getVersion()` 把实际应用版本注入 Sidecar 握手，不再固定上报旧版本；Chrome 扩展继续独立使用 `0.8.2`，未改版本。
- 可见预验收发现：转换后的 MP3 实际为 74458 字节，但旧候选完成卡片、队列与历史仍保留源 MP4 的 54856 字节或 0 字节。当前源码在最终成品验证后同步 `downloaded_bytes`、`total_bytes` 和历史使用的 `video_info.file_size`；自定义脚本修改成品后会再次刷新。
- Bilibili 公网多 P 预验收发现：解析受 HTTP 412 阻断时，确认窗直接展示 yt-dlp 英文原始错误，且没有任何可选条目时“开始下载”仍呈可用状态。当前源码只显示“暂时无法解析此链接，可尝试网页识别”，统一使用“网页识别”产品名称，并在没有可下载条目时禁用主按钮；底层原始错误继续留在进程日志与测试证据中，不作为界面文案暴露。

先前 `desktop/release` 中的 Windows 安装包生成于 Electron 握手修复之前。它仍固定向 Sidecar 上报 `0.2.1`，与 `0.2.4` 包版本不一致，现仅保留为历史证据，**不得再作为候选安装或发布**。之后的 `desktop/release-v0.2.4-handshake-fix` 修复了握手，但被完成大小问题取代；`desktop/release-v0.2.4-final-size-fix` 又因上述解析失败界面问题被取代。最新包写入独立目录 `desktop/release-v0.2.4-parse-ui-fix`，没有覆盖或删除前三个目录。早先占用旧包的两个安装器进程已退出，最新精确检查没有发现任何 v0.2.4 安装器进程。三个 `package_smoke` 脚本只负责验收进程的可靠退出，不属于打包应用内容，但这些修改目前同样未提交。

## 自动化回归

以下仅记录本候选上实际执行并成功结束的结果。

| 检查 | 实际结果 |
| --- | --- |
| Electron 握手定向红灯 | 按预期失败：实际 `0.2.1`，期望注入版本 `9.8.7` |
| Electron 握手定向绿灯 | 1 passed in 6ms |
| 最终成品大小定向红灯 | 按预期失败 2 项：普通完成仍为 5/9 字节，脚本完成仍为 0 字节，而最终文件分别为 22/19 字节 |
| 最终成品大小定向绿灯 | 2 passed，46 deselected，2.15s |
| 解析失败界面定向红灯 | 按预期失败：暴露 `HTTP Error 412`、按钮仍写“用浏览器抓取”且“开始下载”未禁用 |
| 解析失败界面定向绿灯 | 3 passed in 483ms；新增用例确认中文行动提示、无原始错误、统一“网页识别”和空选择禁用 |
| Windows smoke 清理定向红灯 | 两轮分别复现：先结束父进程后独立后代仍存活；清理命令真实启动失败产生未处理的 `spawn ENOENT` |
| Windows smoke 清理定向绿灯 | 真实独立后代清理、命令同步启动失败、命令异步启动失败、无法终止时明确报错四项均通过 |
| `pytest tests/core/test_download_manager.py -q` | 48 passed in 23.07s |
| `pytest tests/sidecar/test_protocol.py -q` | 4 passed in 0.30s |
| `pytest tests/core tests/data tests/sidecar tests/cli -q` | 安装后补验最新复跑 394 passed in 49.42s；使用隔离数据目录 |
| `NODE_OPTIONS=--no-experimental-webstorage; npm.cmd test` | 交付前最新复跑 55 test files passed；338 tests passed；43.67s |
| `npm.cmd run build` | 交付前最新复跑通过；Renderer 71 modules / 2.96s，Electron Main 84 modules / 632ms，两个 preload 各 9ms |
| `node shared.test.js` | `shared.js title tests passed` |
| `node sniff-core.test.js` | `sniff-core.js tests passed` |
| `node bridge-timeout.test.js` | `bridge enqueue timeout test passed` |
| `node --test scripts/package_smoke_helpers.test.mjs scripts/test_packaged_media_tools.test.mjs` | 清理修复后最新复跑 19 passed，0 failed；569.0643ms |
| 必需本地媒体工具模式运行 `tests/core/test_media_pipeline_integration.py` | 5 passed in 14.18s |

非门禁探索结果不计入通过证据：一次把完整 Python 与桌面回归并行运行，固定 3 秒等待在争用下超时；随后隔离测试连续 5 次通过，当时顺序全量 Python 回归为 390 项，加入两条最终大小回归后为 392 项，本次再补充两条真实存储重建回归后为 394 项。桌面测试曾漏设 Node 25 的 `--no-experimental-webstorage`，13 项均在 `localStorage.clear()` 的测试初始化阶段失败；本次界面修复后直接运行又复现相同 13 项环境失败，按既定参数顺序重跑后最新 338 项全部通过。另有数次未带完整兼容 GPU 参数的 Electron 探索启动在当前受控会话中退出，不计为候选门禁；使用同一候选和受控软件 GPU 参数的真实界面复验通过。

交付前差异审查对 Windows smoke 清理做了两层受控复现：普通父子进程最初均能退出，但这个结果不足以排除系统进程组联动的影响；把后代改为 `detached: true` 后，旧实现先结束父进程并立即返回，独立后代仍然存活，回归测试按预期失败。当前实现先在父进程仍存活时执行 `taskkill /T /F`，命令不可用或失败时才回退所持进程句柄。随后用真实不存在的可执行路径又复现了异步 `spawn ENOENT` 未处理问题；等待逻辑现同时处理 `exit` 和 `error`，并在完成后移除监听器与计时器。两项修复均完成红灯到绿灯验证，没有修改应用运行时代码或重新生成候选包。

修复后真实独立后代测试和同一最新候选的包级 smoke 均通过；独立进程与监听端口复核确认没有 Downany、DownanySidecar 或 17888/19325/19326 验收端口残留。失败回退测试证明所持父进程句柄可被终止，不扩大为任意权限条件下的后代退出保证；这些辅助脚本证据也不替代安装态退出验收。

### 安装后独立增量审查（2026-08-27）

独立审查以 `c7709bcc64035c18cf94efcfc62b40ad088b8f24` 为基线，只读检查当时的 13 个 tracked diff、新增 `sidecar.test.ts` 及本验收记录；没有运行测试、操作应用、联网或修改文件。版本注入、最终大小写入顺序、解析失败界面和 smoke 退出分支均未发现证据充分的新增缺陷，Critical / Important / Minor 均为 0，`git diff --check HEAD` 通过。非阻断建议是增加重建真实 `HistoryDB` / `QueueStore` 后的大小断言；现有两条定向回归主要断言内存对象。此静态审查不替代安装态旅程、macOS 实机、提交或合并授权。

### 最终成品大小持久化补验（2026-08-27）

已落实上述非阻断建议：新增两条参数化回归，同步执行真实 `DownloadManager` 完成流程、成品提交和 SQLite 写入，再重新构造 `HistoryDB`、`QueueStore` 与管理器。分别以手工确定的 22 字节音频成品、脚本修改后的 19 字节成品，核对磁盘、历史记录、恢复任务和界面快照中的最终大小及路径。网络下载、媒体探测、封面生成和外部脚本是测试替身；真实持久化与文件提交没有被替换。

初审发现测试遗漏自动封面生成支线的隔离。修正先将 `DOWNANY_DATA_DIR` 指向 `tmp_path` 并拦截外部命令，确认两例因意外 ffmpeg 调用被拒绝而失败，再补齐封面替身。最终代码不会运行真实媒体程序或写入用户资料目录。独立复审确认该项已解决，没有新增 Critical / Important 问题；这不是新的产品缺陷，也没有修改生产源码。

| 补验 | 实际结果 |
| --- | --- |
| 测试隔离保护红灯 | 2 failed in 1.20s；严格命令替身拒绝意外封面调用，未运行真实媒体程序 |
| 修复前模块红灯 | 在独立 Python 进程载入基线管理器，2 failed in 0.57s；重新读取的历史大小仍为 7，分别不等于 22/19 |
| 当前源码定向绿灯 | 2 passed in 0.93s |
| 管理器、成品合同及真实存储回归 | 83 passed in 24.29s |
| 控制器独立全量 Python 复跑 | 394 passed in 49.42s，退出码 0；`DOWNANY_DATA_DIR` 指向本工作树被忽略的 `.build/v024-size-persistence/full-regression-data` |

原有 13 个 tracked 修改及 `sidecar.test.ts` 共 14 个文件的 SHA-256 与补验前完全一致；候选包没有重新生成，也没有执行暂存、提交、合并或发布。`13:14:42` 只读复核仍为已安装 `0.2.4`，配置与已安装 `app.asar` 哈希保持上文记录值，应用进程和 `17888` 监听数均为 0。测试补强不替代安装态重启、公开站点旅程或 macOS 实机验收。

## Windows 最新隔离包级候选证据

### 主机、文件与版本链路

- 主机：Windows 10 Home China 22H2，build `19045.6466`，AMD64，64 位系统。
- 隔离输出：`desktop/release-v0.2.4-parse-ui-fix/Downany-0.2.4-win-x64.exe`
- 字节数：`199499008`
- SHA-256：`fe2c8c0dbeb3dc7589280393b8db1d8968fe846fd80951e37ae3ccf15de47ae4`
- 文件时间：`2026-08-27T02:40:33+08:00`
- Authenticode：`NotSigned`
- `app.asar/package.json`：`downany-desktop` `0.2.4`。
- 包内 Electron Main 不含旧版本字面量 `0.2.1`；编译结果使用 `app.getVersion()` 构造 Sidecar，并由 `this.opts.appVersion` 写入 peer hello。
- `app.asar` 的 Renderer 资产包含“暂时无法解析此链接，可尝试网页识别”和“网页识别”，不再包含“用浏览器抓取”。
- 打包 Sidecar 实际首行：`app=Downany`、`appVersion=0.2.4`、`protocolVersion=1`。
- 包内 `DownanySidecar.exe`：13397986 字节，SHA-256 `465b3d08e57b78fd19f733773b3343f1c29c518ba6b1e983b0be4ce054215be7`。
- 当前资源仍是 cloud-only：包内没有 Telegram Bot API / ProcessHost 原生文件；本轮用于本地候选安装，不宣称本地 Bot API 的大文件上传能力。

### 覆盖安装前静态核对

- 覆盖前，机器的卸载注册项为 `Downany · 百纳` `0.1.0`，卸载命令指向 `D:\downany\Uninstall Downany.exe /allusers`；当时 `D:\downany\Downany.exe` 尚未被候选替换。
- 候选继续使用 `appId=com.jacklee.downany`、`productName=Downany`、`executableName=Downany` 和卸载显示名 `Downany · 百纳`；NSIS 为非一键安装并允许选择安装目录，因此可以在真实安装时明确复用现有 `D:\downany`。
- 产品用户数据目录是 `%LOCALAPPDATA%\Downany`，不位于 `D:\downany`；自定义卸载宏只清理由当前安装拥有的协议注册项，并只尝试删除已经为空的安装目录，没有删除用户数据目录的逻辑。
- 以上只证明配置层没有主动清空用户数据或更换应用身份；真实覆盖、SmartScreen/UAC、快捷方式、协议注册和重启后的数据保留仍必须由安装态验收确认。

### Windows 覆盖安装与首次启动（2026-08-27）

用户本轮明确确认“现在关闭旧安装器并安装 v0.2.4 最新候选”。启动前核实旧安装器已全部退出，只启动了 SHA-256 为 `fe2c8c0dbeb3dc7589280393b8db1d8968fe846fd80951e37ae3ccf15de47ae4` 的最新 NSIS。向导可访问文本明确显示 `Downany 0.2.4`，并识别“已经存在一个安装到所有用户的安装.(D:\downany) 即将重新安装/升级.”。

本机窗口截图调用返回 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`；使用可访问文本继续核对。安装器切换窗口后只暴露标题，未盲点不可见控件，也未自动操作系统安全提示。后续系统复核证明覆盖已完成，安装器全部退出，实际安装版主窗口已打开。没有把短暂的窗口读取限制误记为安装失败；但本轮没有可用的 SmartScreen/UAC 画面证据，不对其具体交互作通过声明。

| 安装态检查 | 实际结果 |
| --- | --- |
| 系统卸载登记 | `Downany · 百纳` `0.2.4`；仍使用原 HKLM 安装识别键及 `"D:\downany\Uninstall Downany.exe" /allusers` |
| 已安装主程序 | `D:\downany\Downany.exe`；SHA-256 `8bca1e7d887ffde73153e2fa90f288518a1ca4bb225843d1c52c3d8dc1a99bfa`，与批准候选一致 |
| 已安装应用包 | `resources/app.asar`，9015896 字节；SHA-256 `c39896c8a1bea9268fbc615300a11d772ab62e67466da311c509cd175efff526`；包内 `downany-desktop` 版本为 `0.2.4` |
| 配套文件 | 已安装 Sidecar、ffmpeg、ffprobe、yt-dlp 的字节数与 SHA-256 均逐项等于同一候选对应文件 |
| 正常首次启动 | 实际主进程 PID `25564`，路径 `D:\downany\Downany.exe`；未使用 `--no-sandbox`、软件 GPU 验收参数或 `--user-data-dir` 隔离参数 |
| Sidecar / 扩展桥 | 安装目录内 Sidecar PID `20168`；`GET http://127.0.0.1:17888/health` 返回 `ok=true`、`service=downany-bridge`、`sidecarReady=true` |
| 实际界面 | 标题“Downany · 百纳”；原有 1 个队列任务恢复为“已完成”，显示 8.2 MB，添加、网页识别、设置、下载记录等入口存在 |
| 开始菜单 | `C:\ProgramData\Microsoft\Windows\Start Menu\Programs\Downany.lnk` 指向 `D:\downany\Downany.exe`，无额外参数 |
| 协议注册 | `HKCU\Software\Classes\downany\shell\open\command` 为 `"D:\downany\Downany.exe" "%1"`；此项是注册内容核对，不冒充已点击真实深链接 |

安装前将 `%LOCALAPPDATA%\Downany` 的顶层配置和数据库复制到被忽略的 `.build/v024-preinstall-backup-20260827-122711`，逐项验证备份哈希。配置 `config.json` 为 680 字节，SHA-256 `e61d2a66704728b77762f9edddab97dd0457a8ef5202af3d864635a53bc7ac06`，覆盖安装及首次启动后均保持一致。原 `history.db` 为 61440 字节，备份 SHA-256 `82bc3e66d474853bb3df24f7cd23dc7c9b0fd62ceb603f199796ee9df40fc4bd`。

数据库在首次启动迁移后物理哈希发生变化，因此另以 SQLite 只读连接逐表、逐个旧列比较数据，而非把哈希变化当作数据丢失：4 条 `download_history`、1 条 `task_queue` 的全部原始字段均保留，原本为空的 `search_history`、`telegram_delivery_queue`、`telegram_delivery_target_blocks` 仍为空；仅 `download_history` 与 `task_queue` 增加 `completion_note` 列。`PRAGMA integrity_check` 返回 `ok`。

已安装路径下的 ffmpeg 与 ffprobe 还实际生成并探测了 0.5 秒、同时含视频和音频的 MP4：`.build/v024-installed-media-smoke-20260827/sample.mp4`，6562 字节，SHA-256 `4bbf338260255141eee948e9d8283264920ead828f174008363df31784242325`。样本和备份均保留，没有修改用户下载成品。本次安装态基础验收通过，不自动替代下方仍欠缺的公开站点、系统默认播放器、退出/重启与 macOS 完整旅程。

### 安装态正常退出与重启检查（2026-08-27）

- 退出前只读核对用户配置：`menu_bar_mode=false`、`telegram_auto_send_enabled=false`、`postprocessing=none`，自定义脚本为空；队列只有 1 个已完成任务，下载历史仍为 4 条，没有进行中的任务。
- 向已观察到的安装版窗口发送一次正常关闭快捷键 `Alt+F4`，没有使用强制终止。`12:46:16` 的独立检查已无 `17888` 监听，但部分进程仍在退出；`12:46:28` 再检查，`Downany.exe`、`DownanySidecar.exe` 与该端口监听数均为 0。因此只记录正常退出及最终资源释放通过，不据此推定精确退出耗时。
- 随后分别通过精确已安装路径 `D:\downany\Downany.exe` 和系统返回的应用 ID `com.jacklee.downany` 执行桌面工具启动，两次均返回 `computer-use request timed out: launch_app`。每次操作后都重新观察状态，没有盲目重复启动；应用列表为 `isRunning=false`，进程及本地服务均未出现。普通开始菜单的键盘打开尝试也没有返回可继续操作的菜单窗口。
- 本机截图此前已返回 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`；这次关闭前的安装版窗口只能读到原生菜单，未读到 Renderer 文档。以上是自动化观察与启动限制，尚不足以诊断为产品崩溃，也不能将重启标为通过。已停止继续发送启动输入，没有更改 GPU/沙箱参数或用户配置来掩盖问题。
- 当前安装版已关闭，需恢复普通桌面启动后才能继续安装态下载、文件打开和重启恢复旅程。本次没有新增用户下载任务、删除用户成品或修改系统关联；首次安装时已验证的版本、文件同一性和旧数据保留仍成立。

### Windows 范围调整后的复核（2026-08-27）

用户排除 Mac 工作后，本轮仅同步规格、计划与记录，并尝试一次已安装 Downany 的普通启动。精确应用名称匹配为 Downany，注册 ID 为 `com.jacklee.downany`；桌面工具仍返回 `computer-use request timed out: launch_app`，随后列表没有 Downany 窗口，也没有可用于替代启动的文件资源管理器窗口，因此停止继续发送输入。

`14:54:17` 的独立只读检查：安装登记仍为 `0.2.4`，配置与 `app.asar` 的 SHA-256 保持安装记录值；Downany、Sidecar、候选安装器进程及 `17888` 监听数均为 0。本轮未重新安装或打包，未新增用户下载任务，未修改用户配置，也未提交或发布。该结果只证明自动启动未完成，不足以把根因归为产品崩溃。Mac 不再列为阻塞；Windows 重启与后续完整旅程仍未通过。

### Windows 启动阻塞的只读排查（2026-08-27 19:10）

本轮没有重复执行已超时的 Downany `launch_app`，也没有重新安装、重打包或修改生产代码。先检查当前进程、用户和机器三个范围的 `ELECTRON_RUN_AS_NODE`、`ELECTRON_NO_ASAR`、`NODE_OPTIONS`、`ELECTRON_ENABLE_LOGGING`，均未设置；当日 Application 事件 1000/1001/1002 中未检索到匹配 Downany/Electron 的记录。这些结果只排除了已检查项，没有证明应用启动正常或确定此前超时的根因。

与下午不同，此时桌面工具返回了一个资源管理器窗口。精确匹配已有资源管理器后，尝试经普通文件浏览打开已核验的安装目录，但在进入目录前已遇到控制接口错误：

- 读取窗口可访问文本成功；点击已观察到的地址栏返回 `coordinate input geometry is unavailable`。
- 刷新窗口后，`Ctrl+L` 没有产生可观察的焦点变化；没有向仍聚焦的搜索框盲目输入路径。
- 再次读取地址栏后，使用其可编辑控件写入目录返回 `read UIA value read-only state: 所需属性不在 CacheRequest 中 (0x80070057)`，未观察到目录切换或 Downany 启动，随后停止输入。

`19:10:45` 独立复核：Downany/Sidecar 进程及 `17888` 监听仍为 0；已安装 `Downany.exe`、`app.asar` 与用户 `config.json` 的 SHA-256 分别仍为 `8bca1e7d887ffde73153e2fa90f288518a1ca4bb225843d1c52c3d8dc1a99bfa`、`c39896c8a1bea9268fbc615300a11d772ab62e67466da311c509cd175efff526`、`e61d2a66704728b77762f9edddab97dd0457a8ef5202af3d864635a53bc7ac06`。未新增下载任务或修改系统关联。这次证据确认当前桌面控制路径受限，不能据此判定 Downany 产品崩溃；安装态重启仍未通过，需恢复普通桌面启动后继续。Mac 仍不在本任务范围。

### 媒体工具同一性

替代包 `win-unpacked` 与源码资源目录中的对应文件具有相同字节数和 SHA-256：

| 工具 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `ffmpeg.exe` | 138577920 | `c807e1c7d098049511cb9ebda3b3ef09fd49cd86194f807a92f4c82c11e040d8` |
| `ffprobe.exe` | 138440704 | `8f74a42dd61221f423d9c33f10d3517639bd2f0b0f9dea6f1becc835185279ba` |
| `yt-dlp.exe` | 18388925 | `78a3ac4cd1eeb681d65e55fc1761ee14c87de8d8699afb09140d1049c15ae006` |

### 替代包 smoke

- `win-unpacked` 媒体工具 smoke：通过，真实生成并探测含视频与音频的 MP4 样本。
- 打包 Sidecar 协议、媒体工具诊断和隐私安全诊断 smoke：通过，退出码 0。
- 打包 Electron 首次使用 smoke：通过，退出码 0；观察到任务 `c5e442dd-e842-4df9-a720-42e7eca60598`，状态 `pending`。
- smoke 持有的 Electron PID `14020` 已退出；之后的 Bilibili 失败态复验使用独立数据目录与进程，该实例也已正常退出。
- 进程树清理修复后的最新首次使用 smoke：同一二进制通过，退出码 0；Electron PID `15868`，任务 `4b91f041-a95d-487b-934c-bd7dbe760e55`，状态 `pending`。隔离数据保留在被忽略的 `.build/v024-process-tree-smoke-20260827`，没有触碰现有用户数据或安装目录。结束后独立复核 Downany/DownanySidecar 进程数与验收端口监听数均为 0；NSIS 和未安装主程序的 SHA-256 与上文完全一致。

### Bilibili 公网解析失败态复验

- 公开候选源：`https://www.bilibili.com/video/BV1224y1Q7K9/`。Bilibili 公开元数据接口当日返回 HTTP 200、业务 `code=0`；该 BV 自身为单集（`videos=1`），但所属 `season_id=351804` 的 `ugc_season` 包含 15 个 episode，因此适合作为“合集”而非裸多 P 的旅程来源。
- 当前主机通过既有网络路径执行候选包内 yt-dlp 与真实添加界面时均得到 HTTP 412；另两个公开 BV 链接同样为 412，关闭代理后的直连被当前主机网络策略拒绝。因此不能把它判定为单一视频下架，也不能声称多 P 下载通过。
- 最新 `win-unpacked` 的实际确认窗显示“暂时无法解析此链接，可尝试网页识别”，不包含 `HTTP Error 412`；行选择禁用，“开始下载”禁用，“网页识别”入口可用。
- 可见截图保存在被忽略的 `.build/v024-bilibili-ui-fix.png`，不属于候选源码或发布资产。

### 被取代的握手修复包

`desktop/release-v0.2.4-handshake-fix/Downany-0.2.4-win-x64.exe`（199499126 字节，SHA-256 `72da766b934ad7130d8ccf6c94afbf329784eb77146a1333f12980c6573132c1`）通过了握手和包级 smoke，但可见预验收发现它没有把转换后最终文件大小写回任务与历史。因此它仅保留为问题复现证据，**不得安装或发布**。

### 被取代的完成大小修复包

`desktop/release-v0.2.4-final-size-fix/Downany-0.2.4-win-x64.exe`（199498950 字节，SHA-256 `49809befa3fd95ae46afe131072106b38256d9f3af7f122c8be550e6c37736b2`）完成过 MP4 / MKV / MP3 与 MDN 公网单视频预验收，但 Bilibili 失败态仍暴露原始工具错误且允许空选择提交，因此 **不得安装或发布**。媒体模式与公网单视频现已在最新 `fe2c8c0d…de47ae4` 包上重跑；本节旧结果只保留为问题演进记录，不再承担最新候选证据。

## 历史 Windows 包级证据（对应已作废安装包）

以下证据仅说明握手修复前的包曾通过包级检查，不能替代新源码的重新打包与重新验收，也不能清除任何候选门禁。

### 主机与安装包

- 主机：Windows 10 Home China 22H2，build `19045.6466`，AMD64，64 位系统。
- 已作废文件：`desktop/release/Downany-0.2.4-win-x64.exe`
- 字节数：`199499081`
- SHA-256：`3853e5e373bd65cc70671388a2a4c507106867966649b7a5524dbd7009031ef2`
- 文件时间：`2026-08-26T17:41:08.9333754+08:00`
- Authenticode：`NotSigned`
- 打包模式：`BUILD_TELEGRAM_NATIVE=0`、`ALLOW_CLOUD_ONLY_PACKAGE=1`。
- `app.asar/package.json`：`downany-desktop` `0.2.4`。
- 打包 Sidecar `hello`：`app=Downany`、`appVersion=0.2.4`、`protocolVersion=1`。

### 媒体工具同一性

源码资源目录与 `win-unpacked` 中的对应文件具有相同字节数和 SHA-256：

| 工具 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `ffmpeg.exe` | 138577920 | `c807e1c7d098049511cb9ebda3b3ef09fd49cd86194f807a92f4c82c11e040d8` |
| `ffprobe.exe` | 138440704 | `8f74a42dd61221f423d9c33f10d3517639bd2f0b0f9dea6f1becc835185279ba` |
| `yt-dlp.exe` | 18388925 | `78a3ac4cd1eeb681d65e55fc1761ee14c87de8d8699afb09140d1049c15ae006` |

### 包级 smoke

- 源码资源媒体工具 smoke：通过。
- `win-unpacked` 媒体工具 smoke：通过。
- 打包 Sidecar 协议与隐私安全诊断 smoke：通过，退出码 0，19.5489s。
- 打包 Electron 首次使用 smoke：通过，退出码 0，2.2458443s；观察到任务 `e49bf4d5-95bd-4d1d-8fce-95b6b9a6dfe4`，状态 `pending`。
- Electron smoke 原先在业务断言通过后因 `taskkill` 权限失败残留进程并以清理错误退出。当时改为先使用所持子进程句柄、再回退 `taskkill`，对应 2 项回归测试和真实打包 smoke 曾通过；交付前新增的独立后代测试证明该顺序仍可能留下后代，现已被上文“进程树优先、句柄回退并处理异步启动错误”的实现取代。本段只保留历史，不再作为当前清理行为的说明。

### 历史 Windows 覆盖安装门禁（已被本轮安装结果取代）

**当时未通过。** 该次检查时系统仍登记并运行旧安装基线：

- 注册项：`Downany · 百纳`，版本 `0.1.0`。
- 卸载命令：`"D:\downany\Uninstall Downany.exe" /allusers`。
- 安装识别键：`HKLM\Software\1c5b01c0-7b3b-5df5-9608-c39d71f5f2c0`，`InstallLocation=D:\downany`；相同键在 `HKCU` 下不存在。
- 已安装主程序：`D:\downany\Downany.exe`，188784128 字节，SHA-256 `ee821037f1101addb985e8b0a9fe6015080b7d6110f9d0f657561d3aedf7ef54`，文件时间 `2026-08-07T00:53:20.2994011+08:00`。
- 两个安装器进程仍存在：PID `13644`（启动于 2026-08-26 19:23）和 PID `21808`（启动于 2026-08-26 19:25）；最新复核时二者 `MainWindowHandle=0`，没有可见安装窗口。当前沙箱无法控制其提权上下文。
- 这两个进程均来自已经作废的旧安装包；在它们收敛前不启动第三个安装器，避免并发覆盖安装造成状态不确定。
- 没有观察到 `0.2.4` 覆盖安装成功，因此不能声称旧数据保留、安装后首次启动或“打开/在文件夹中显示”已验收。

覆盖路径的静态核验已通过，但不能替代真实安装：

- `v0.1.0` 与当前候选的 `appId` 均为 `com.jacklee.downany`；electron-builder 25.1.8 按固定命名空间计算出的 NSIS `APP_GUID` 均为 `1c5b01c0-7b3b-5df5-9608-c39d71f5f2c0`，与当前 HKLM 安装识别键一致。
- 当前为 assisted installer（`oneClick: false`）。其 `initMultiUser` 会同时检查 HKLM/HKCU：当仅发现 HKLM 旧安装时，自动调用 `setInstallModePerAllUsers`，读取原 `InstallLocation`，并在安装模式页默认选择“所有用户”。
- 因此现有配置不会因 `selectPerMachineByDefault` 的默认值而把这次单一 HKLM 升级误导为并排的 per-user 新装；无需为该项修改 `electron-builder.yml`。

## 历史 Windows 未安装包可见预验收

以下是在已被解析界面修复取代的 `release-v0.2.4-final-size-fix/win-unpacked/Downany.exe` 上通过实际界面执行的预验收，不替代最新二进制、NSIS 覆盖安装、公开站点或完整七旅程门禁。最新包没有改变 Sidecar 或输出合同，但仍不会把前序二进制的可见结果自动记为最新候选通过。

- 日期 / 主机：2026-08-27，Windows 10 Home China 22H2，build `19045.6466`，AMD64。
- 应用构建：被取代安装包 SHA-256 `49809befa3fd95ae46afe131072106b38256d9f3af7f122c8be550e6c37736b2`。
- 隔离数据根：`%LOCALAPPDATA%\Temp\downany-v024-ui-qa-final-size-20260827`。
- 来源：本机 `127.0.0.1:19324` 提供的 3 秒受控媒体；它们只用于确定性验证输出合同，不替代公开网站旅程。
- 首次 MP3 请求因本机系统代理自动检测而失败，界面显示“网络连接失败，请检查网络或代理后重试”和“检查网络设置”。通过该实际入口保存直连状态后点击“重试”，同一任务完成，没有重复添加任务。

| 模式 | 受控来源 | 最终相对路径 | 最终文件与媒体证据 | UI / 持久化 / 重启 |
| --- | --- | --- | --- | --- |
| MP3 | `Downany-visible-QA-mp3-source.mp4`；54856 字节；SHA-256 `58a735de4b4dfd0bf295464084925d14aab2a788dd1895f01423a308042cace4` | `output\Downany-visible-QA-mp3-source [url-c9e81be4].mp3` | 74458 字节；SHA-256 `bde5eec5772f6f1c7a0b28b930a4c49a542a5334dc2738a049c480bd90ddfb2f`；MP3 音频，44100 Hz 单声道，3.056327s | 卡片 `72.7 KB`；队列与历史均为 74458；重启后恢复 |
| MKV 回退 | `Downany-visible-QA-fallback.mkv`；615999 字节；SHA-256 `43acd72324268d427c08de766887095df542f2f6d903ef677a375ef643530f57` | `output\Downany-visible-QA-fallback [url-06c508b5].mkv` | 616196 字节；SHA-256 `0fe2f01c70293095483852ae27b545e8ad48a6e77d87a169b77850a2cb3b889f`；Matroska，MPEG-4 640×360 + AAC 44100 Hz，3.064s | 卡片 `601.8 KB`；队列与历史均为 616196；重启后恢复 |
| 默认 MP4 | `Downany-visible-QA.mp4`；54856 字节；SHA-256 `58a735de4b4dfd0bf295464084925d14aab2a788dd1895f01423a308042cace4` | `output\Downany-visible-QA [url-82423105].mp4` | 54999 字节；SHA-256 `c831c01a4fa0b892d615cc52cd13490cedf1b6274a94d8f088fce51862f8cc37`；MP4，MPEG-4 640×360 + AAC 44100 Hz，3.0s | 卡片 `53.7 KB`；队列与历史均为 54999；重启后恢复 |

三项任务完成与重启后的窗口均为 1104×695，页面 `scrollWidth=1104`、`scrollHeight=695`，没有文档级溢出。包内协议的中文任务标题与数据库 `未知` 也通过 UTF-8 原始字节及 Unicode 码点复核；先前终端中出现的乱码只是 Windows Python 诊断输出编码，不是产品数据损坏。

“打开”和“在文件夹中显示”仍只形成部分证据：MP3 的实际“打开”按钮把文件交给了系统关联程序，但当前账户的 Windows Media Player 尚未完成首次设置，只启动 `setup_wm.exe`，没有观察到播放器窗口或播放结果；记录证据后已停止本轮启动的该辅助进程，没有代替用户完成播放器设置。“在文件夹中显示”菜单已实际触发，但当前非提权会话无法读取 Explorer 的选中项。两项都不能记为正式通过。

## 历史 Windows 公网单视频预验收

以下同样在被取代的 `release-v0.2.4-final-size-fix/win-unpacked/Downany.exe` 上通过实际界面执行，使用独立数据根 `%LOCALAPPDATA%\Temp\downany-v024-public-qa-20260827`。这补充了公网传输与恢复证据，但不替代最新二进制、NSIS 覆盖安装、富元数据页面或系统播放器门禁。

- 成功来源：MDN 公共 CC0 视频 `https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4`。
- 最终文件：`output\flower [url-8a390855].mp4`，1131116 字节，SHA-256 `269e2f409249ec6866c29464a1c85e983472d196236af8bd03319c3061975e10`。
- `ffprobe`：MP4，H.264 960×540 + AAC 48000 Hz 双声道，5.055s；容器 `title=flower`、`date=20251017`，来源 URL 写入 `comment`。
- 完成卡片显示 `flower`、`1.1 MB`；封面协议图片加载完成，实际尺寸 960×540。队列的 `downloaded_bytes`、`total_bytes`、`video_info.file_size` 与历史 `file_size` 均为 1131116，历史 `output_state=ready`。
- 输出目录只包含上述一个最终 MP4，没有残留分片或临时成品。关闭并以同一数据根重启应用后，6 个任务全部恢复；成功任务仍显示 `flower`、`1.1 MB`，封面仍为 960×540。
- 重启所需的 `--no-sandbox --disable-gpu` 仅用于当前受控桌面会话的 GPU 兼容；不属于应用配置或候选修改。未带 `--no-sandbox` 的两次探索启动因当前会话 GPU 子进程不可用而退出，不计为产品门禁失败。

同一隔离会话还保留了公网失败分类证据，均未生成输出文件，也未被误报为成功：

| 来源 | Sidecar 原始结果 | UI 结果 |
| --- | --- | --- |
| yt-dlp 官方测试视频 `BaW_jenozKc` | `Video unavailable`，`error_code=removed` | “此内容已被删除或不可用” |
| YouTube `jNQXAC9IVRw` | 下载媒体数据时 HTTP 403，`error_code=network` | “网络连接失败，请检查网络或代理后重试” |
| Vimeo `56282283` | 获取 macOS OAuth token 时 HTTP 401 | “下载未完成，请重试或改用网页识别” |
| W3C Sintel MP4 | Cloudflare anti-bot HTTP 403，`error_code=network` | “网络连接失败，请检查网络或代理后重试” |
| GitHub Raw Big Buck Bunny MP4 | 读取超时 15 秒，`error_code=network` | “网络连接失败，请检查网络或代理后重试” |

本轮没有再次触发“打开”，因为同一主机上的 Windows Media Player 首次设置阻塞已在受控媒体预验收中确认；因此公网单视频旅程仍只记为部分通过。

## Windows 最新候选媒体与公网补验

以下在最新 `release-v0.2.4-parse-ui-fix/win-unpacked/Downany.exe` 上通过实际添加入口、设置页、任务卡片和重启流程执行。对应 NSIS SHA-256 为 `fe2c8c0dbeb3dc7589280393b8db1d8968fe846fd80951e37ae3ccf15de47ae4`，本次实际启动的 `win-unpacked/Downany.exe` 为 188784128 字节、SHA-256 `8bca1e7d887ffde73153e2fa90f288518a1ca4bb225843d1c52c3d8dc1a99bfa`。隔离数据根、输出根和 Electron 用户数据分别为 `.build/v024-latest-media-data`、`.build/v024-latest-media-output`、`.build/v024-latest-media-userdata`；本机受控媒体由 `127.0.0.1:19326` 提供，取证后服务已停止。启动参数中的 `--in-process-gpu --disable-gpu --use-gl=swiftshader` 只绕过当前验收会话的 GPU 子进程依赖，不属于产品配置或候选修改。

| 模式 / 来源 | 最终相对路径 | 最终文件与媒体证据 | UI / 持久化 / 重启 |
| --- | --- | --- | --- |
| 默认 MP4 / 本机 3 秒 H.264 + AAC | `media [url-bb1efa8f].mp4` | 32643 字节；SHA-256 `630b190c3703ba084e335b9105d77cc0a00127717bddae0fb588fad2fc13cff1`；MP4，H.264 640×360 + AAC 44100 Hz，3.000s | 卡片 `31.9 KB`；队列、历史与磁盘均为 32643；`output_state=ready` |
| MKV 回退 / 本机 3 秒 Matroska | `fallback [url-201e56fd].mkv` | 31129 字节；SHA-256 `fbf17017439581c81a2b23f8e5cde666ff1f820cc6f26699302b24dd679aaf19`；Matroska，H.264 640×360 + AAC 44100 Hz，3.041s | 卡片 `30.4 KB`；队列、历史与磁盘均为 31129；`output_state=ready` |
| MP3 / 本机 3 秒 MP4 音源 | `audio-source [url-31f7a127].mp3` | 74391 字节；SHA-256 `82afe4cb58c40e512252462e3b76b3430ae167b7c83c3ba21428f5c3fb9f7a55`；MP3 44100 Hz 单声道，3.056327s，无视频流 | 实际设置页切换“提取音频 (MP3)”；卡片 `72.6 KB`；队列、历史与磁盘均为 74391；`output_state=ready` |
| 公网单视频 / MDN CC0 `flower.mp4` | `flower [url-8a390855].mp4` | 1131116 字节；SHA-256 `269e2f409249ec6866c29464a1c85e983472d196236af8bd03319c3061975e10`；MP4，H.264 960×540 + AAC 48000 Hz，5.055s；`title=flower`、`date=20251017`、来源 URL 写入 `comment` | 卡片 `flower`、`1.1 MB`；960×540 封面加载完成；队列、历史与磁盘均为 1131116；`output_state=ready` |

四项任务完成后，输出目录精确包含上述四个最终文件，没有残留分片或临时成品。关闭并使用同一数据根重启最新候选后，四项均恢复为“已完成”，三张可用封面仍分别加载为 640×360、640×360 和 960×540；窗口为 1104×695，页面 `scrollWidth=1104`、`scrollHeight=695`，没有文档级溢出。

播放器形成了分层证据：从最新候选点击 MP4 的“打开”后，系统默认关联再次启动 `C:\Program Files (x86)\Windows Media Player\setup_wm.exe`；当前账户尚未完成 Windows Media Player 首次设置，因此没有进入播放器，记录后已停止本轮启动的辅助进程。随后在不改系统关联的前提下，用本机 5KPlayer 6.9.0.0 打开最新 MP4 成品，播放器可访问状态同时显示文件名 `media [url-bb1efa8f]`、当前进度 `00:00:01` 和总时长 `00:00:03`，确认 MP4 已实际开始播放；同一播放器精确选择 MKV 后，文件窗口无错误关闭并显示 `00:00:00 / 00:00:03`，确认它已识别并打开该 3 秒成品。5KPlayer 对 MP3 只显示 `00:00:00 / 00:00:00`，不作为通过证据；改用 PotPlayer 64 bit（`PotPlayer64.dll` 1.7.22038）从干净进程直接载入同一 MP3，关闭但未安装其更新提示后，主窗口标题为 `audio-source [url-31f7a127].mp3 - PotPlayer`，确认精确成品已打开。由此第 7 项要求的三种成品“在至少一个常见播放器中打开”已有对应证据；只有 MP4 观察到实际播放进度，应用自身经系统默认关联打开的链路仍未通过。

## Windows 列表、路径与字幕可见预验收

以下在最新 `release-v0.2.4-parse-ui-fix/win-unpacked/Downany.exe` 上通过实际设置页、添加入口、选集面板、任务卡片和分组删除界面重新执行，使用隔离数据根 `.build/v024-latest-visible-data` 与输出根 `.build/v024-latest-visible-output`。本机 `127.0.0.1:19325` 提供两条不同的 3 秒 MP4、两条普通 RSS 列表记录及带 `zh-Hans` 字幕的 4 秒 HLS；服务和候选应用均已停止。生成脚本、媒体、数据、输出与截图都位于被忽略的 `.build`，不属于候选源码或发布资产。为避免当前系统代理把回环请求转发到外部，实际设置页先填写 `http://127.0.0.1:9` 再关闭“启用代理”，由现有配置语义明确选择直连。

### 普通列表、分组恢复与精确删除

- 规格覆盖的 `/lists/feed.xml` 经实际添加入口打开“确认下载”面板，显示 `Downany 可见列表`、`已选 2/2`、条目 `01/02`；点击“开始下载”后建立同一 `group_id=eeda41e1-4ff5-4bb0-8ed8-39edfe533c23` 的两个任务。
- 独立目录 `Downany 可见列表 [bdf7eaae]` 中依序生成 `001 - same [url-e11c2033].mp4`（32716 字节，SHA-256 `6c4a5bb54744ec043355ee604164b54fb5646b904ac9e519f272b55775157ba1`）和 `002 - same [url-20c5c0be].mp4`（32451 字节，SHA-256 `8a7be36a3d2dc9e7e8dd7789de1d6a42a69defca8f3c697ae4e012d9433c2028`）。卡片显示 `2/2 完成 · 100%`。
- 关闭并重启同一数据根后，13 个任务恢复；分组仍为一组，条目仍按 `列表条目甲`、`列表条目乙` 的 1/2 顺序显示，四种字幕结果及缺失语言提示也保持。
- 在实际分组删除确认中勾选“同时删除已下载的本地文件”后，两个队列任务、两个跟踪文件及空分组目录均消失；队列由 13 行降至 11 行，分组行降至 0。无关控制文件 `same [url-57a96a3f].mp4` 仍为 32664 字节且 SHA-256 仍为 `1483aedecadcf928ee83a5fa671e08d107254104e57cd5515f316a702c97111a`。下载历史仍保留 13 行，其中两条既有分组记录保留，未被伪装成队列任务。

### 重名、非法字符、长标题与模板

| 场景 | 最终文件 | 字节数 | SHA-256 | 观察结果 |
| --- | --- | ---: | --- | --- |
| 同标题内容 A | `same [url-57a96a3f].mp4` | 32664 | `1483aedecadcf928ee83a5fa671e08d107254104e57cd5515f316a702c97111a` | 与内容 B 同时存在，队列/历史大小一致 |
| 同标题内容 B | `same [url-cb8c336b].mp4` | 32399 | `1baf9a64fd093a6c15263be6285be84900a3195334d93710f6b06dab6c009fe2` | URL 指纹不同，没有覆盖内容 A |
| Windows 非法字符 | `非法：名称＊问号？引号＂小于＜大于＞｜ [url-bbce3ceb].mp4` | 32828 | `b0d2a315b13cd58e86c4aedc6ead41fa95e541a68b89bf0b6f98c3e1b3fcdab3` | 卡片保留原始可读标题，磁盘名使用安全全角字符 |
| 275 字标题 | 截断后的 120 字文件名，完整路径 207 字 | 35700 | `0f9fb7fe247863f8d1edec0a63e61aaa9c582aeb1ac76a54d0a34bc0835c1b6e` | 下载完成、文件存在，保留 URL 指纹与扩展名 |
| `验收-%(title)s-%(id)s.%(ext)s` | `验收-same-same [url-ba2d711b].mp4` | 32671 | `e3cd45843a51cc994bd033bcbdbc5dd3ef5735b2302c1fd0c554fee8d36c4660` | 实际设置页保存模板后生效 |

### 字幕四种结果

| 模式 / 语言 | 主文件 | 成品证据 | 独立字幕 / UI |
| --- | --- | --- | --- |
| 独立 / `zh-Hans` | `master [url-6efce907].mp4`；45964 字节；SHA-256 `3dc0246f39214b6bba05083f124bdd929af3468ca84a7a7d40d25c2368793366` | H.264 + AAC，无字幕流 | 同名 `.zh-Hans.vtt`，65 字节 |
| 内嵌 / `zh-Hans` | `master [url-ce89660f].mp4`；46506 字节；SHA-256 `a598d7054ba870eb27a98e1ccbc6dd917bde8f8ab49538a7b622e59c63c273f8` | H.264 + AAC + `mov_text` 字幕流，语言 `zho` | 无独立字幕 |
| 两者并用 / `zh-Hans` | `master [url-cdabb795].mp4`；46502 字节；SHA-256 `1ff5afe9bdb0b45e1da455538aa06feff16648bf317345333a0ee85d3815a3bf` | H.264 + AAC + `mov_text` 字幕流，语言 `zho` | 同名 `.zh-Hans.vtt`，65 字节 |
| 独立 / 不存在的 `fr` | `master [url-73b5b277].mp4`；45959 字节；SHA-256 `f01c41c245369b86689caa2732fef64c42b4a9433a5a16532edc256b9d5e6609` | H.264 + AAC，无字幕流 | 任务保持完成；卡片和持久化提示“未找到所选语言字幕” |

删除分组前的 13 个完成任务均满足 UI 大小、队列 `downloaded_bytes/total_bytes`、历史 `file_size` 与磁盘字节一致；其中两个 `/illegal.mp4`、`/long.mp4` 短路径探索任务只用于校准标题来源，不替代上表的动态中文标题合同。重启后的窗口为 1104×695，页面 `scrollWidth=1104`、`scrollHeight=695`，没有文档级溢出。由于这些是本机受控媒体且未经过 NSIS 安装，它们只记为可见预验收，不替代相应真实公开站点与安装态门禁。

## macOS 候选证据

未执行。当前没有 Apple Silicon macOS 实机证据，未生成或核验本候选 DMG，也未执行 `.app` 包级 smoke、真实安装、Gatekeeper 行为或系统播放器检查。此门禁不能由交叉编译、模拟结果或旧版本结果替代。

## Windows 可见验收旅程

自动化测试不能替代以下可见旅程。第 1、2、4、5、6、7 项均已在最新未安装包上形成对应预验收，其中第 1 项完成公网直接媒体闭环，第 2、4–7 项包含受控源合同；安装态、应用默认关联打开链路和指定真实站点仍有缺口。第 3 项已在最新包上执行解析失败态，因外部 HTTP 412 未进入选集或下载。所有正式门禁仍未满足。

| # | 旅程 | 日期 / OS / 构建 | 来源 | 最终路径与媒体证据 | UI / 播放器 | 观察结果 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 公共单视频：下载、来源元数据/封面、系统播放器打开 | 2026-08-27 / Windows 10 19045.6466 / `fe2c8c0d…de47ae4` | MDN CC0 `flower.mp4`（最新未安装包预验收） | 1131116 字节；SHA-256 `269e2f40…1975e10`；H.264 960×540 + AAC；标题、日期、来源注释与重启恢复通过 | 卡片 `flower`、1.1 MB、960×540 封面通过；应用“打开”止于 WMP 首次设置 | 下载闭环通过；播放器门禁未满足 |
| 2 | YouTube 播放列表：选择条目、独立目录、重启恢复、打开子项、删除分组且只删除其跟踪文件 | 2026-08-27 / Windows 10 19045.6466 / `fe2c8c0d…de47ae4` | YouTube 公共单条当日 HTTP 403；用两条本机普通列表替代验证分组合同 | 32716 / 32451 字节；独立目录、顺序、重启和精确删除通过，无关控制文件哈希不变 | 选集与分组 UI 通过；未确认 YouTube 列表与播放器 | 部分执行；门禁未满足 |
| 3 | Bilibili 多 P / 合集：顺序、中文名称、重启恢复 | 2026-08-27 / Windows 10 19045.6466 / `fe2c8c0d…de47ae4` | 公开 15 段视频 `BV1224y1Q7K9`；当前网络路径 HTTP 412 | 未创建任务或输出 | 中文行动提示、原始错误隐藏、空选择禁用通过；未进入选集 | 已尝试；外部门禁未满足 |
| 4 | 普通网站列表：解析、分组、至少两个输出 | 2026-08-27 / Windows 10 19045.6466 / `fe2c8c0d…de47ae4` | 本机 RSS `/lists/feed.xml`（未安装包预验收） | 两个 MP4 位于同一独立目录，ffprobe 与哈希通过；重启恢复 | 选集 `2/2`、分组 `2/2 完成` 通过 | 受控源通过；公开普通站点门禁未满足 |
| 5 | 同标题不同内容、非法字符、长标题、自定义模板 | 2026-08-27 / Windows 10 19045.6466 / `fe2c8c0d…de47ae4` | 本机两条不同 MP4 与动态标题（未安装包预验收） | 两个同名输出并存；非法字符安全转换；275 字标题截断；自定义模板生效 | 卡片保留可读原始标题；重启恢复 | 受控源通过；安装态门禁未满足 |
| 6 | 外挂字幕、内嵌字幕、两者并用、请求语言不可用 | 2026-08-27 / Windows 10 19045.6466 / `fe2c8c0d…de47ae4` | 本机带 `zh-Hans` 的 HLS（未安装包预验收） | 外置 VTT、内嵌 `mov_text/zho`、两者并用均通过；无 `fr` 时主文件仍有效 | 四种模式经实际设置页执行；缺失提示重启后保持 | 受控源通过；公开源/安装态门禁未满足 |
| 7 | MP4、MKV 回退、MP3，并在常见系统播放器中打开 | 2026-08-27 / Windows 10 19045.6466 / `fe2c8c0d…de47ae4` | 本机 3 秒受控媒体（最新未安装包预验收） | MP4 32643、MKV 31129、MP3 74391 字节；三项打包内 ffprobe、UI/队列/历史大小一致与重启恢复通过 | 5KPlayer 6.9.0.0：MP4 `00:00:01 / 00:00:03`，MKV `00:00:00 / 00:00:03`；PotPlayer 1.7.22038 主窗口显示精确 MP3 文件名；应用“打开”仍止于 WMP 首次设置 | 输出合同及三格式常见播放器打开通过；系统默认关联链路未满足 |

## macOS 历史验收清单（本任务不负责）

| # | 旅程 | 日期 / OS / 构建 | 来源 | 最终路径与媒体证据 | UI / 播放器 | 观察结果 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 公共单视频：下载、来源元数据/封面、系统播放器打开 | —（未执行） | —（未执行） | —（未执行） | —（未执行） | 本任务不负责，不阻塞 |
| 2 | YouTube 播放列表：选择条目、独立目录、重启恢复、打开子项、删除分组且只删除其跟踪文件 | —（未执行） | —（未执行） | —（未执行） | —（未执行） | 本任务不负责，不阻塞 |
| 3 | Bilibili 多 P / 合集：顺序、中文名称、重启恢复 | —（未执行） | —（未执行） | —（未执行） | —（未执行） | 本任务不负责，不阻塞 |
| 4 | 普通网站列表：解析、分组、至少两个输出 | —（未执行） | —（未执行） | —（未执行） | —（未执行） | 本任务不负责，不阻塞 |
| 5 | 同标题不同内容、非法字符、长标题、自定义模板 | —（未执行） | —（未执行） | —（未执行） | —（未执行） | 本任务不负责，不阻塞 |
| 6 | 外挂字幕、内嵌字幕、两者并用、请求语言不可用 | —（未执行） | —（未执行） | —（未执行） | —（未执行） | 本任务不负责，不阻塞 |
| 7 | MP4、MKV 回退、MP3，并在常见系统播放器中打开 | —（未执行） | —（未执行） | —（未执行） | —（未执行） | 本任务不负责，不阻塞 |

## 剩余限制与保留状态

- Windows NSIS 未签名；实际覆盖安装已完成，但本轮未取得 SmartScreen/UAC 具体交互的可用画面证据。
- 当前候选为 Telegram cloud-only 打包模式，不含本地 Telegram native 资源。
- 握手、最终大小与解析失败界面修复后的最新安装包位于 `desktop/release-v0.2.4-parse-ui-fix`，已覆盖安装到 `D:\downany`；原 `desktop/release`、`release-v0.2.4-handshake-fix` 和 `release-v0.2.4-final-size-fix` 都已被取代，不得安装或发布。
- Windows 覆盖安装、安装文件同一性、首次启动、原数据保留及安装路径媒体工具检查已通过。七条旅程仍沿用各自明确标注的预验收边界：第 1 项已有 MDN 公网直接媒体证据，第 2 项已有受控普通列表合同证据，第 3 项仍止于 HTTP 412，第 4–7 项已有受控媒体预验收；其中第 7 项有三格式常见播放器打开证据和 MP4 实际播放进度。指定公开列表、多 P 下载、应用默认关联打开以及在安装版上完成全部旅程仍有缺口。macOS 构建、安装和七条可见旅程全部未执行；按用户最新要求，这些项目不属于本任务，也不再阻塞 Windows 版本推进。
- 安装版正常退出后所有进程和 `17888` 端口已释放；桌面控制工具的精确路径及注册应用 ID 启动均超时，没有产生可观察的新进程，因此安装态重启未通过，后续真实界面验收暂不能继续。该限制没有被当作产品启动失败的已证实根因。
- Windows Media Player 首次设置阻塞了应用“打开”后的实际播放确认；没有擅自替用户完成播放器首次设置。5KPlayer 与 PotPlayer 已分别覆盖三种精确成品的打开证据，但这不替代应用按钮经系统默认关联打开的安装态门禁。
- 工作树和本记录尚未提交；Task 14 的 Windows 门槛尚未全部满足，且本轮没有取得新的提交授权，因此没有提前提交候选记录。
- 三个本轮失败 smoke 遗留的、由测试命名的临时目录仍保留：`downany-packaged-electron-mJOfVQ`、`downany-packaged-electron-5nc7er`、`downany-packaged-electron-measure-Odxaga`。精确清理命令被当前策略在执行前拒绝，因此未删除任何目录。
- 旧版 `0.1.0` 已被批准的 `0.2.4` 候选覆盖，系统登记已更新。本轮明确确认后的最新安装器已退出；安装版已完成正常首次启动并在退出验收中正常关闭，当前不在运行。没有强杀安装器或绕过安全提示。安装前配置/数据库备份保留在 `.build/v024-preinstall-backup-20260827-122711`。

## 仓库与发布状态声明

本次没有执行合并、推送、打 tag、分支或 worktree 清理、网站部署、GitHub Release 创建、公开产物上传或正式发布。当前记录不授权任何这些操作。
